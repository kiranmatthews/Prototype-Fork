# Level iteration log

| # | Brief | Prompt → local playable | Pages deployment | Clarifications | Files | Result and playtest notes |
| --- | --- | ---: | ---: | ---: | --- | --- |
| 0 | Fork baseline and publishing setup | Setup phase | Setup phase | 0 | Workflow, fork config, docs, validation | Baseline production build and browser smoke test passed. |
| 1 | Long mixed-flow switchback: winding skate descents, dedicated side-scroll climb, straight ridge, and on-foot uphill platforming | ~33 min | Automatic from `main` | 0 | `src/levels/codex-lab.ts`, `src/level.ts` | 1.28 km longitudinal span; 575 components; 215-node camera spine (8 m maximum spacing); one tightly bounded side-scroll zone; five board-speed gaps; 16-piece foot climb; eight checkpoints. Replaced repeated berm boxes with one spline-driven swept wall per side, continuous UVs and smooth longitudinal normals; collision uses 2 m maximum intervals with adaptive curve subdivision. Lite and full-render browser passes, checkpoint warps, live side-scroll handling, finish approach, console, level validator, type-check and production build passed. |
| 2 | Replace every web sea implementation with the complete Unity Beachfront ocean, shoreline and effects stack | ~30 min | Automatic from `main` | 1 | `src/unityOcean.ts`, `src/coastpost.ts`, `src/water.ts`, `src/waterstudio.ts`, `src/level.ts`, `src/main.ts`, MatrixRex assets | Removed the four-wave coastline solver, swash/wet sheets and Slipstream plasma shader. Ported the two audited Gerstners, curved ribbon, deep coverage, horizon fill, planar reflection, opaque color/depth, refraction, caustics, depth foam, normals/specular, coastal sand and mask, solid shallow-water boundary, polygon deep-water fallback, Unity atmosphere, bloom and lens profile. Full/lite browser passes, repeated level switching, high-road and shoreline views, animated surface, legal-shallows stop, asset hashes, validator, bundle and console/WebGL checks passed. |
| 3 | Unity gameplay/QoL backport: movement, slides, bails, crates, board/trick rules, reusable mechanics, and procedural timber/bamboo authoring | ~75 min | Automatic from `main` | 0 | `src/player.ts`, `src/tuning.ts`, `src/level.ts`, `src/editor.ts`, `src/main.ts`, `src/ui.ts`, `src/levels/backport-lab.ts` | Added 0.75/0.45 walking inertia, the 11/0.55 double jump, exact five-metre slide, generic fold-over and huge-drop bails, corrected crate/Slam/Arrow rules, intentional deck recall, emergency eject, multi-flip and grind approach rules, optional Endless Deaths, seven reusable editor primitives, and spline wood/bamboo paths with seamless collision. Added Backport Mechanics Lab. Level/ocean validators, type-check, production build, lite/full browser passes, primitive visuals, selectable rule UI and console checks passed. |
| 4 | Exhaustive editor no-op and round-trip hardening | ~120 min | Automatic from `main` | 0 | `src/editor.ts`, `src/level.ts`, `src/main.ts`, `tools/validate-editor-roundtrip.mjs`, `tools/validate-levels.mjs` | Added transactional hidden pick proxies so open/select/close preserves the exact live level, player, replay, pause, split-screen, camera, fog, sky/RNG and fixed-step state; pure sparse-default property rendering; complete new-primitive rotate/scale/resize fields, including runtime-matched banked path scaling; gesture rollback; per-level editor views; safe import/group normalization; surfaced storage failures; helper-resource disposal; and automatic editable copies for lossy hand-built courses. Added lite/full data, runtime, malformed-group and sparse-primitive round-trip gates plus real-browser no-op selection, transform, paused-state, hand-built-level and console passes. |
| 5 | Editor-native procedural bendy walls and Jungle Ruins wall replacement | ~35 min | Automatic from `main` | 0 | `src/level.ts`, `src/editor.ts`, `tools/validate-editor-roundtrip.mjs`, `tools/validate-levels.mjs` | Added the `wallpath` open/closed spline/fillet primitive with continuous swept geometry and UVs, height/thickness/collision/visibility/material controls, path-aware wallrides, complete editor knot/property/transform support and capture-safe identity. Replaced the Jungle's stair-stepped earth-bank boxes with 18 smooth scenery sweeps and its 20 repeated temple/finish boxes with four footprint-exact structural wall paths; retained independent pit liners. |
| 6 | Port six current Unity courses, including the initial Beachside boardwalk islands, Bonus parallax, right-side Coastal beach, Island/Jungle routes, and fair glowing Meshy thorns | ~45 min | Automatic from `main` | 0 | `src/levels/*-run.ts`, `src/levels/island-hopper.ts`, `src/levels/bonus-level.ts`, `src/levels/meshylook-thorns.ts`, `src/bonusParallax.ts`, `src/proceduralThorns.ts`, `src/visual-treatment/` | Added six source-owned, editor-compatible levels. Beachside initially had 28 procedural wood paths in seven four-island sequences with 2.75–5.25 m jump gaps and sand-connected access ends; Coastal retained all 23 road grades/four gaps and moved the shared beach treatment to screen right; Island retained 11 joined timber paths and two rope crossings; Jungle retained its exact side-view platforms, kickers and required rails; Bonus used its registered four-layer camera-space painting; Meshy used visible code-native pulsing thorns over separate 1.15×1.25 m lethal cores. Added one global LOOK tab for grading/bloom/vignette source presets. |
| 7 | Restore MeshyLook's actual stone bridge art and forward-looking camera | ~30 min | Automatic from `main` | 0 | `public/meshy/ancient-stone-courtyard.glb`, `src/meshyCourtyard.ts`, `src/levels/meshylook-thorns.ts`, `src/level.ts`, `src/editor.ts`, `tools/bake-meshy-courtyard.py` | Replaced the visible procedural banks with four exact-transform instances of the owner-supplied Meshy Ancient Stone Courtyard while retaining the 16 ramps as hidden deterministic collision. Reduced the 41.38 MiB source handoff to a canonical, core-glTF 127 KB GLB (1,065 triangles, embedded 512 px JPEG, no decoder extension), added cached shared loading/editor controls/provenance, and removed the E travel zone so the existing +X spine drives the authored behind-the-rider shot. Browser measurement matched Unity's spawn camera exactly (`camera.x=-8.438585`, rider `x=-3.538585`, zero lateral offset); the actual bridge, four glowing thorn wells, supported spawn, one network fetch and clean console were verified. |
| 8 | Merge Beachside with the exact Unity Beachfront, join every boardwalk and restore Stonecliff Bastion | ~45 min | Automatic from `main` | 0 | `src/beachfront*.ts`, `src/levels/beachfront-run.ts`, `src/level.ts`, `public/beachfront/`, boardwalk/cliff validators | Removed the duplicate approximate menu row, blue ocean slabs, broad pits and procedural boulders. The canonical Beachside now combines the literal 371×65 sand, 821-row MatrixRex ocean/post path and 149-node camera lane with seven continuous 16-knot boardwalks, source actors, curved coast/cliff collision and all 150 Stonecliff transforms. The decoder-free cliff GLB retains 2,270 triangles yet is only 158KB. Runtime rays hit all 21 former boardwalk gaps; full/lite ocean diagnostics, repeated level switching, source hashes, editor capture, production build and clean browser console passed. |
| 9 | Authorized clean-room reconstruction of Kraftpaper's Crash NST Maker “Jungle Cliff” mod | ~75 min | Automatic from `main` | 0 | `tools/iga_igz_layout.py`, `src/levels/jungle-cliff.ts`, `docs/JUNGLE_CLIFF_PORT.md`, focused importer/level validators | Parsed the 2,398-member IGA v11 archive read-only, recovered 442 camera objects, six camera splines, 192 positioned crate records and the 501-entry static-collision map, then rebuilt the layout with existing procedural primitives only. The playable course retains the winding jungle rise, fixed west-facing cliff section, upper temple corridor, finish elevation and a condensed portal-linked death route; 106 camera nodes, 31 crates plus three checkpoint boxes, source special-crate families and supported checkpoint plinths fit within 279 components. No source art/audio/mesh data ships. Synthetic importer tests, real-archive inventory/camera dump, level/editor round trip, full suite, TypeScript, production build and lite/full browser/console checks passed. |
| 10 | Coherent campaign shell: launch/save flow, row warp room, pause/options, Crash-style bonuses, progression, game over and run results | ~35 min | Automatic from `main` | 0 | `src/campaign.ts`, `src/gameFlowUI.ts`, `src/main.ts`, `src/player.ts`, `src/level.ts`, canonical level data, audio/HUD tests | Added three durable save slots with four-life/zero-fruit recovery, stable canonical progress keys, crystal/gem/relic totals and clear+crystal run-mode unlocks. Rebuilt the hub as a safe HUD-less nine-portal gallery with Roo labels and fade travel; every canonical course now has a run-local bonus platform linked to a suspended-parent copy of the 34-box bonus, preserving checkpoints, loose rewards, RNG and global inventory while making bonus deaths free. Added interactive mouse/keyboard/gamepad launch, pause thumbnail, split audio options, results and 3D bone-mask game-over screens; M now hides all authoring chrome. Added missing canonical crystals, time-relic targets, final-box settling, modal input isolation, mobile/short-viewport handling and campaign regression coverage. |
| 11 | Recover pre-campaign frame rate and reduce campaign GPU memory pressure | ~20 min | Automatic from `main` | 0 | `src/main.ts`, `src/level.ts`, `src/swirls.ts`, `src/gameFlowUI.ts`, campaign/HUD performance tests | Confirmed normal travel owns one level and only Bonus deliberately retains its parent. Distance-gated the long portal row so only nearby gates animate/render, skipped all hidden hub HUD and M-hidden debug DOM work, held one gameplay frame behind modal screens instead of redrawing/copying it every RAF, removed fullscreen backdrop blur, avoided duplicate gameplay gamepad scans, and throttled/downsized/released the Bone Mask WebGL context. Bonus suspension now detaches its exact-state parent and drops recreatable ocean targets; Roo label backing textures are 75% smaller. Full suite, TypeScript, production build, lite/full browser passes, 0-frame paused-world sample and clean console passed. |
| 12 | Campaign follow-up: persistent gate presentation, midpoint bonuses, temporary bonus economy, gate trophies and mobile pause | ~35 min | Automatic from `main` | 0 | `src/level.ts`, `src/main.ts`, `src/campaign.ts`, `src/swirls.ts`, `src/touch.ts`, `src/input.ts`, `src/gameFlowUI.ts`, campaign/mobile/editor validators | Replaced pop-prone whole-gate distance hiding with conservative frustum culling plus low-poly, hysteretic frozen-frame swirl LOD: every arch, label and portal face remains present while only nearby deformation streams. Added four instanced trophy sockets per gate for saved crystals, box gems, combo gems and time relics. Default bonus platforms now project spawn/finish onto each real 3D camera route (or ordered checkpoints), search supported collision around 50%, and return locally; all nine canonical placements validated at 47–50% route progress. Bonus runs now begin at zero temporary lives/fruit, never spend a life on failure, discard the purse on death, and merge lives/wumpa—including cross-boundary 100-fruit rollover—only on clear. Added a safe-area, semantic 48px touch Pause button routed through the canonical input edge plus a compact landscape pause layout. Browser award, touch pause/resume, full/lite console, runtime placement and production-build checks passed. |
| 13 | Reuse the preserved Gouraud sine-field vortex for title, loading and Game Over backgrounds | ~25 min | Automatic from `main` | 0 | `src/gameFlowVortex.ts`, `src/gameFlowUI.ts`, `src/main.ts`, `src/swirlfield.ts`, campaign presentation tests | Promoted the pre-band `FIELD_SWIRL_PRESETS.vortex` lab effect into one persistent presentation stage. Its 361-vertex, two-pass textureless field now fills the title/save stack, staged loading transitions and Game Over on the same 60 Hz presentation clock as gameplay. Loading fades black into the live field, holds a complete frame through synchronous level construction and paints the destination before reveal. The 3D skull/crossbones moved into the same renderer and loads once per session, removing repeated Game Over WebGL contexts and model parsing while retaining a PNG failure fallback. Zeroed vortex features skip unused sine work and all streamed attributes use dynamic GPU hints. Animated title, visible loading handoff, combined mask/vortex, full/lite build and clean-console browser passes succeeded. |
| 14 | Unify all main-canvas presentation at 60 Hz | ~10 min | Automatic from `main` | 0 | `src/gameFlowVortex.ts`, `src/main.ts`, game-flow/render-quality tests | Removed the nested 30 Hz timestamp gate from the shared vortex stage so title, loading, Game Over and active play all draw on the single `GAMEPLAY_RENDER_HZ = 60` scheduler. This also removes timestamp-quantization aliasing between two cadence gates. Vortex time now consumes the real accepted-frame delta on high-refresh/uncapped paths, and presentation renderer state restores inside `finally` so an exceptional UI draw cannot contaminate gameplay. Exact 60 Hz limiter tests, shared-stage ownership tests, TypeScript, production build and browser handoff diagnostics passed. |
| 15 | Add a metal Nitro `!` clear crate at the end of every Nitro-bearing level | ~20 min | Automatic from `main` | 0 | `src/level.ts`, Nitro switch regression tests | Added one derived, uncounted green metal detonator on a supported finish approach whenever a level owns Nitro boxes. The rule runs per Level instance, so bonus stages clear only their own Nitros and receive a switch only when their own layout needs one. Existing authored switches remain authoritative; generated switches stay out of captured level data. Detonation, exact box tallying, reset/retrigger behavior, rotated gates, parent/bonus isolation, all built-ins and every published level are regression-tested. |
| 16 | Restore performance after the Gouraud game-flow integration with explicit render/asset lifetimes | ~35 min | Automatic from `main` | 0 | `src/gameFlowVortex.ts`, `src/gameFlowUI.ts`, `src/main.ts`, `src/coastpost.ts`, `src/bonusParallax.ts`, presentation/lifecycle tests | Proved the game-flow field never ticked during gameplay, then removed its residual and peak costs structurally without reducing its quality: title/loading/Game Over retain full drawing-buffer resolution and the shared 60 Hz presentation cadence. The host owns zero THREE resources while inactive and destroys the complete stage at gameplay handoff. Gameplay composer/SMAA/Bloom/CRT targets collapse to 1×1 before the vortex allocates and restore under the opaque fade without reloading LUTs or shaders, so the two full-quality render graphs never coexist. Sky residency is bounded to the active preset with stale loads cancelled and both canvas/GPU storage released on eviction; the unused startup sky load is gone. Bonus parallax's four large layers load only on bonus entry, settle behind the loading screen, and dispose on return. The inactive transition curtain leaves the compositor entirely. Diagnostics verified mutually exclusive post/vortex ownership, one cached sky, no bonus assets in ordinary play, and sustained 60 Hz gameplay after handoff; focused tests, full/lite browser visuals and console checks passed. |
| 17 | Remove redundant entry splashes and honor previously collected level rewards | ~15 min | Automatic from `main` | 0 | `src/main.ts`, `src/level.ts`, `src/player.ts`, campaign presentation/collectible tests | Removed the generic level-name and “Bonus Round” centre-screen splashes; the portal/Bonus HUD already communicates both states, while checkpoint, mode and banked/lost reward feedback remains. Campaign save progress now projects silently into the live Level after hard run setup: an already-owned crystal stays collected and hidden across restarts and an already-owned box gem cannot materialize or score again, while fresh rewards retain their normal pickup/reset behavior. The committed crystal baseline also survives shared two-player resets; time-trial and combo replay objectives remain available. |
| 18 | Add save-aware Continue to the launch menu | ~10 min | Automatic from `main` | 0 | `src/campaign.ts`, `src/gameFlowUI.ts`, campaign flow/presentation tests | The launch menu now puts Continue first whenever at least one valid durable save exists, making it the default keyboard/gamepad selection and routing through the same fade, inventory restore, warp-room load and asset preparation as Load Game. The last successfully created/loaded slot is remembered across reloads; pre-existing saves fall back deterministically to the newest valid slot. Empty, malformed, stale and ephemeral slots cannot expose Continue. |
| 19 | Make the Gouraud Field Lab context-aware | ~30 min | Automatic from `main` | 0 | `src/fieldstudio.ts`, `src/fieldStudioPresets.ts`, `src/gameFlowVortexProfiles.ts`, `src/gameFlowVortex.ts`, context/lifecycle tests | Split the full-screen field into independent Menu/Title, Warp/Loading and Game Over source profiles and added a persistent four-workspace lab with a preserved Scratch draft. Context selection restores its exact controls, template and seed; Reset affects only that context, while Copy Active, Copy All and Download All produce promotion-ready output. Legacy single lab drafts migrate safely into Scratch. The preview now privately owns one field, survives level-system clears, pauses without reallocating and releases replaced topology buffers. Runtime context changes use immutable lazy stages, derive cover from the active radius, allocate skull-mask resources only for Game Over, remain direct full-resolution at 60 Hz, and still release every presentation resource for gameplay. `#fieldstudio` now opens the lab directly. |
| 20 | Promote the tuned Game Over Gouraud profile | ~5 min | Automatic from `main` | 0 | `src/gameFlowVortexProfiles.ts` | Replaced only the Game Over field recipe with the supplied 35-segment, six-arm, deep red/black profile at seed 37. Menu/Title and Warp/Loading retain their previous source profile. No local test or build run was performed at the requester’s direction. |
| 21 | Promote the tuned Warp/Loading Gouraud profile | ~5 min | Automatic from `main` | 0 | `src/gameFlowVortexProfiles.ts` | Replaced only the Warp/Loading field recipe with the supplied fast-swallowing eight-segment blue/red profile at seed 37. Menu/Title and Game Over retain their existing source profiles. No local test or build run was performed at the requester’s direction. |
| 22 | Add safe Warp Room saves and shader-composited game menus | ~55 min | Automatic from `main` | 0 | `src/campaign.ts`, `src/gameFlowUI.ts`, `src/gameFlowSurface.ts`, `src/coastpost.ts`, `src/gameFlowVortex.ts`, campaign/render tests | Warp Room pause now offers Save / Load, Options and confirmed Quit to Main Menu. Autosave defaults on but can be toggled in Save / Load; active progress is a deep working copy, durable slots are isolated snapshots, failed writes stay dirty, destructive loads require confirmation, and quitting always offers Save & Quit or explicit discard. Game-owned menus retain their real DOM buttons for mouse/keyboard/gamepad/accessibility while a dirty-cached Canvas2D mirror is inserted after LOOK and before CRT. Pause/results keep LOOK on the world only; title/loading/Game Over use one LOOK-free input while sharing the existing CRT/Output owner. Debug tools and the cartoon cursor remain sharp DOM. Full-size GameFlow storage is released at gameplay handoff, and browser smoke confirmed the complete save/quit flow, shader separation and clean console. |
| 23 | Remove the remaining bonus-return splash | ~5 min | Automatic from `main` | 0 | `src/main.ts`, campaign presentation test | Removed both the “Bonus Complete” and “Bonus Missed” centre-screen announcements. Bonus completion still banks boxes, lives and fruit, failure still discards temporary rewards, and both paths immediately expose the authoritative parent-level HUD after the loading transition. |
| 24 | Repair shader-composited menu interaction and presentation | ~35 min | Automatic from `main` | 0 | `src/gameFlowUI.ts`, `src/gameFlowSurface.ts`, GameFlow interaction/render tests | Replaced transition-sampled menu geometry with fixed hit rectangles and one discrete selected state, eliminating frozen split highlights and width jumps. DOM-to-pre-CRT ownership now changes atomically only after a successful Canvas draw; pause hides its semantic visual during the clean thumbnail frame and uses a fixed 16:9 cover crop rather than stretching or reflowing after capture. M-visible developer chrome is restored from modal inert/ARIA state and elevated above GameFlow but below the fade curtain, while the full-screen semantic shell passes pointer input through to real buttons and debug tools. Focus, scrolling, element/visual-viewport resize and fallback paths now invalidate deterministically. Disabled opacity is applied once, and the menu raster is capped near 2 MP while still compositing to the full CRT/Render target. |
| 25 | Make zero lives the final playable life | ~10 min | Automatic from `main` | 0 | `src/player.ts`, `src/campaign.ts`, campaign death/save-flow tests | Defined the HUD number as reserve lives: a death at one spends the reserve and respawns on the playable zero-life attempt; only a death that begins at zero reaches Game Over. The terminal decision is latched at impact so delayed wumpa rewards cannot change it during the blackout, and the counter never becomes negative. Save normalization now preserves a zero-life final attempt and repairs only invalid negative data. Executable coverage preserves checkpoint respawn plus the separate time/combo, free-bonus and endless-death rules. |
| 26 | Split normal and time-trial run results | ~20 min | Automatic from `main` | 0 | `src/campaign.ts`, `src/main.ts`, `src/gameFlowUI.ts`, results/campaign tests | Replaced the ambiguous all-purpose results payload with explicit normal and time-trial variants. Normal clears show boxes and newly obtained crystal/gems with no elapsed time; completed trials show only Your Time against Relic Target plus the shared retry/continue actions. Ordinary clears no longer write time-trial best times, while a dedicated trial commit owns best-time/relic progress without manufacturing normal clears. All nine canonical relic targets temporarily share an exact 60-second value, including equality. |
| 27 | Return from each level at its own Warp Room gate | ~25 min | Automatic from `main` | 0 | `src/level.ts`, `src/player.ts`, `src/main.ts`, `src/swirls.ts`, warp-return tests | Pause Quit Level, Game Over No and results Continue now capture the originating campaign identity and hard-respawn outside that gate; New Game/Load retain the gallery entrance. Stable progress keys preserve Test Course fallback and future level replacements. Each gate owns one local trigger, swirl and live return transform; placement derives projected body clearance, raycasts real floor support, faces outward, preserves the hub movement frame and snaps character/camera interpolation. Bonus quits resolve through their parent level, and authored yaw/scale keep the gate trigger, swirl and return aligned without per-frame matrix inversion. |
| 28 | Restore debug level selection in production | ~10 min | Automatic from `main` | 0 | `src/main.ts`, campaign regression test | Removed the production/playtest and open-menu rejection from the left debug panel's level callback. Explicit level selection now uses the existing loading transition, clears pending completion, restores parent inventory when leaving a bonus, loads assets, and dismisses the game menu. Existing campaign sessions are retained; title-screen testing without an active game creates only an ephemeral session. Browser checks on the full renderer verified title → Sky Bridge and paused Sky Bridge → Nightworks through real debug buttons, with gameplay resumed and no console errors. |
| 29 | Fix walking animation after returning from board air | ~15 min | Automatic from `main` | 0 | `src/player.ts`, locomotion replay regression and two supplied recordings | Reproduced the stale board-air flag selecting the 23-unit skate speed range for 9-unit grounded running, yielding a 91% Walk blend. Animation now uses takeoff provenance only while airborne. Both complete replay movement/inventory traces match their pre-fix hashes; all stuck-walk samples are eliminated. Regression coverage checks full-speed recovery, slow Walk and board-air normalization in Test Course and Slipstream. Browser runtime diagnostics confirmed Run at full foot speed in both reported sections. |
| 30 | Promote the local Chrome tuning to factory defaults | ~20 min | Automatic from `main` | 0 | Movement, CRT, Look, Character Lab, animation catalog, captured-default fixture and regressions | Captured the running Chrome game before refresh, including all 168 movement values, both CRT variants, disabled CRT/Look states, distinct Skull/Roo proportions, alternate head, hidden tail, and 1.6× Run playback. Factory startup and Reset now use the capture; Neutral Look remains an identity preset. Collision calibration is pinned to the original level-authoring silhouette so publishing appearance defaults cannot silently resize the existing browser-tuned hitbox. Historical input-only replay fixtures explicitly pin their original silhouette and retain both movement hashes. Lite and full-render browser smoke checks passed. The full local settings/animation backup remains outside the repository; Chrome preference reset is a separate, scoped operation preserving saves and level edits. |
| 31 | GPT-6 Astra benchmark: a novel skating/platforming level, The Chimeworks | ~25 min | Automatic from `main` | 0 | `src/levels/astra-chimeworks.ts`, registry, opt-in playtest/results integration, course tests, `AGENTS.md` | Added a 707 m floating instrument factory without replacing the existing lab/canonical pack or changing tuning: two broad downhill troughs, seven rising xylophone keys, two optional full-length grind strings, a trampoline solo, crushers, sliding keys, four checkpoints and a final landing balcony. All 520 components remain editor-owned; scenery batches with no new downloaded assets or render systems. Real-Player tests cover 14 jumps, both grinds, both factory-steering descents, boost contact and the bonus-height detour/return; 875 support probes, moving-platform phases, crate seating, Nitro-clear generation and editor round-trip pass. Corrected coplanar trampoline seating, a camera kink and the final receiver during QA. Full build and lite/full production browser smoke checks passed with no console errors. Validated direct playtest links and normal results for noncanonical courses; explicitly permitted optional Blender use going forward. |
| 32 | Independent camera tuning controls | Interrupted/resumed; total prompt time not recorded | Automatic from `main` | 0 | `src/cameraRig.ts`, `src/main.ts`, `src/tuning.ts`, `src/ui.ts`, `src/replay.ts`, camera regressions | Replaced aim-height tilt with explicit pitch in degrees; camera position easing no longer rotates the view. Folded the redundant forward rig offset into actual trailing distance, preserving the default shot and converting deliberate old browser saves and replay edits. Side/reverse/boulder/split shots retain calibrated offsets, P2 now honors tilt, and ordinary Beachfront/boulder FOV responds to the lens control. Labels show units; partial number input and keyboard slider focus are stable. Chrome measured constant 25.35° pitch throughout a +1 m height and +3 m distance edit, and a 15° tilt edit left position unchanged. Save/reset/defaults/reload, old-save conversion and re-save, split screen, side shot, jumping, checkpoint warp and kill-plane respawn passed. Full build and lite/full-render browser checks passed with no console errors. |
| 33 | Seat idle/walking feet without changing Run or skating | ~30 min | Automatic from `main` | 1 | `src/player.ts`, footwear/recovery regressions, Character Lab docs | Measured an approximately 20 cm idle hover and 15 cm mean Walk hover. Added a final rider-only correction using the actual rounded outsole vertices and the current support plane, leaving 6 mm clearance and fading with the existing Walk/Run blend. The user clarified that Run already hides the gap; full Run, skating, board air and foot air remain identical to the previous presentation. Collision, board transforms and authored rig/animation data are unchanged. Real-animation comparisons verify contact, no added penetration and exact protected-pose transforms; the recovery test distinguishes the closing waist-pivot transform from the subsequent walking floor correction. Browser measurements confirmed 6 mm Idle/Walk clearance and an identical Run clearance trace, with a full-render visual check and no console errors. Full build passed. |
| 34 | Hop onto the board for charged and automatic skate mounts | ~25 min | Automatic from `main` | 1 | `src/skateMount.ts`, `src/player.ts`, footwear/head-order regressions, animation docs | Added one 0.30 s, 0.28 m rider hop with knee/arm accents and a 0.14 s landing settle whenever on-foot movement enters skating. Standing and moving direction+X charges keep their existing crouch and commit threshold; automatic momentum/downhill mounts use the same transition. The deck gets a fully seated launch reference to avoid the old first-frame overlap, then rider lift composes after planting. Full skating stance and height return exactly at completion; real jumps, actions, dismount and respawn clear the transient. Paired real-Player tests verify identical physics and board transforms, one-shot behavior, both charges, automatic entry, quick-tap exclusion, interruption and reset. Browser captures show roughly 27 cm of actual sole clearance at the peak for all three paths, clean launch and return to the original deck contact, with lite/full rendering and no console errors. Full build passed. |
| 35 | Accumulating spare boards with comic breakage | ~75 min | Automatic from `main` | 0 | `src/skateboard/discarded.ts`, `src/skateboard/fracture.ts`, board shader, Level/Player/render lifecycle, board regressions and docs | Each discarded board now belongs to its Level, continues independently after remount/respawn or P2 leaving, and remains as decorative clutter until that Level unloads. On first firm impact, a separate cosmetic RNG selects occasional textured two-piece snaps or folded taco boards; gameplay RNG, movement and collisions are unchanged. Sleeping pieces use geometry/material instances in 32 m cells; fracture geometry is cached, and disposal frees only owned resources. Added custom deck-shader instancing support after visual QA caught missing surfaces. Tests cover 120 permanent sleeping boards, 18 snapped fragments, fold reuse, single-step ownership, interpolation, borrowed-resource safety and existing ledge/rope detach behavior. Browser checks verify abandon/remount, repeated bail/respawn accumulation, 40-second persistence, P2 handoff, unload cleanup and full-render griptape/artwork with no console errors. Full build passed. |
| 36 | Warp Room Modern/Classic play-mode option | ~50 min | Automatic from `main` | 0 | `src/campaign.ts`, `src/gameFlowUI.ts`, `src/gameFlowSurface.ts`, `src/main.ts`, campaign regressions, play-mode docs | Added Play mode to Warp Room pause → Options, sharing the existing debug preference and applying the existing endless-deaths rule to normal campaign play. Classic remains the fresh-browser default. The choice persists across reload/Continue without changing hub position, inventory or progress; course menus omit it and the callback rejects non-hub changes. Bonus and replay rule ownership remain separate. Corrected toggle text fitting in the Canvas menu mirror and narrow-screen card padding so long mode values remain legible. Full build passed. A fresh non-debug browser campaign verified selection/persistence, Modern zero-life respawn, Classic Game Over, non-hub restriction, preserved hub state, and desktop/mobile visual layout with no console errors. |
| 37 | Live end-of-run character and 3D reward presentation | ~70 min | Automatic from `main` | 1 non-blocking | `src/resultsPresentation.ts`, `src/player.ts`, `src/level.ts`, `src/main.ts`, GameFlow UI/surface, results tests and docs | Replaced results-only frozen rendering with a presentation-clock scene: looping catching-breath idle without new rewards, or open arms and raised gaze beneath the actual crystal, clear/green gems and shared Warp Room time-relic asset. Finish pad and obstructing systemic Nitro switch art are temporarily hidden, with the skater seated on real end-area support. Box totals remain UI; trial results emphasize run time, the existing 1:00 target and top-three local times including saved campaign bests. No new relic tiers or gameplay tuning. Added camera-space bounds fitting for desktop/portrait/landscape, resource-safe cleanup and preserved pause caching. Full build passed. Tests cover eight reward variants, all nine campaign staging supports, looping without simulation drift, responsive bounds and shared-resource preservation. Lite/full browser passes verify actual finish routing, single trial-history writes, reward combinations, Jungle/Beachside backgrounds, mobile layouts and Retry/Continue with no console errors. |
| 38 | Readiness-driven loading and clean transition handoffs | ~35 min | Automatic from `main` | 0 | `src/presentationLoading.ts`, GameFlow UI/surface, HUD surface, `src/main.ts`, `src/level.ts`, `src/player.ts`, `src/audio.ts`, loading tests/docs | Replaced short fixed loading delays with opaque-black readiness gates: prepare and paint the actual vortex before revealing it, keep it fully visible for at least two seconds while loading, fade back to black only after asset settlement, warm destination pose/camera/shaders/HUD/post, then reveal the clean scene before releasing input. Added nested Three.js request tracking plus canvas portrait/sticker images, character imports/attachments, audio decode, fonts, saved animation data and initial level-data readiness. Ocean reflection art starts before gameplay. Retained existing optional-asset fallbacks, immediate pause/resume, reduced-motion behavior and all movement/simulation rules. Loading retires the menu cursor and restores HUD/menu under black to avoid late popping. Full build passed. Browser tests deliberately delayed character textures and cold Beachside assets, verified cached minimum dwell and opaque stages, entered a real Warp Room portal, held/released direction across arrival, checked portrait/reduced-motion, and completed live results/Continue with no console errors. |
| 39 | Easier default bonus, board spin interactions and bonus/HUD polish | ~40 min | Automatic from `main` | 0 | `src/levels/bonus-easy.ts`, `src/bonusPayout.ts`, Level/Player/UI/flow, debris batches, regressions and docs | Added a 62 m left-to-right default bonus with broad permanent terraces, two 2 m hops, 18 crates and safe life/mask rewards; preserved the original Unity bonus. Bonus travel and completed-run results now use asset-ready black fades without a vortex. Masks and remaining third-mask time carry in/out, including failed bonuses; successful fruit/lives are banked safely then count into the returned parent HUD with rollover handling, including Modern mode. Completed trick copy expires and resets at run boundaries. Spins break intact discarded boards, while a later spin poofs fragments; batched slots compact safely and newborn pieces cannot be erased by the same attack. L2 uses enlarged, uncovered real collectible meshes with radial fitting and portrait counter spacing. Refreshed the hidden spawn-camera ground probe and bonus facing. Validation covers real short jumps, finish-trigger routing, successful/failed mask carry, Classic/Modern payout, trick expiry/reset, sleeping/awake board attacks, batch growth/compaction and desktop/portrait rendering without console errors. |
| 40 | Clear discarded boards between lives | ~10 min | Automatic from `main` | 0 | Debris owner, Level reset, bonus return, board/respawn regressions and docs | Added a reusable pile clear that removes awake boards, sleeping batches and fragments, invalidates stale interpolation/handles, and keeps the Level owner available for new discards without disposing borrowed skateboard assets. Soft death respawns and hard run resets clear the pile. Bonus death clears the suspended parent pile on return; successful bonus detours and ordinary remounts still preserve it. Unit/browser checks cover mixed debris, repeated clearing, new-life spawning/breakage, real death/respawn, restart and both bonus outcomes. |
| 41 | Replace the Warp Room with a graph-driven tropical island world map | ~25 min prompt-to-playable | Automatic from `main` | 0 | `src/worldMap.ts`, `src/worldMapController.ts`, `src/worldMapUI.ts`, campaign/GameFlow/Player/Input/Level integration and regressions | Replaced the free-roam portal gallery with two procedural Pacific-island dioramas over the audited Unity ocean: gradient sand/green shelves, palm batches, mountain ranges, waterfalls, nine supported hubs and two boss-marked island endpoints. Stable progress keys now own prerequisites, an explicit branch/join and reversible route edges; normal clears unlock time-trial UI and the next frontier without changing the V1 save schema. Keyboard, gamepad and touch use one discrete direction per press/tap, with quick touch edges latched across RAFs. Selected trail travel is fully scripted and three connections use the live character rig's canned boardslide pose without exposing mount/trick controls. Quit Level, Game Over No and Results Continue restore the exact originating hub. The always-visible 16:9 shell shows hub status, four saved collectibles and post-clear best/target time, with direct View Progress, Options, Save/Load and confirmed Quit Game actions; the responsive touch shell retains labelled utilities and the existing D-pad/face controls. Full campaign and repository regressions, TypeScript, production build, desktop/touch browser entry/return/menu checks and clean console passed. |
| 42 | Make the world map genuinely ocean-led and finish its presentation plumbing | ~20 min prompt-to-playable | Automatic from `main` | 0 | `src/worldMap.ts`, map controller/UI, campaign saves, input, camera/shadows, map/results/mobile regressions and `docs/WORLD_MAP.md` | Fixed the map shoreline winding that had back-face culled the detailed MatrixRex ribbon and left only its flat horizon filler. Rebuilt the composition as nine data-positioned playable island lobes plus scenic cays over thirteen opaque shallow bathymetry shelves, with full reflection/depth passes, map-scaled Fresnel/normal/caustic ranges, refracted coral, visible projected caustics and shader intersection foam. Added sculpted gradient mountain ranges clear of finale hubs, terrain foundations under every elevated pad, fully capacity-backed instanced tapered palms/coconuts/shrubs/flowers/reefs, layered animated waterfalls, route waypoints, stepping-slab trails, dimmed locked rails and supports, and closer selected-hub/travel camera staging with a broader map shadow frustum. Map focus now persists backward-compatibly in V1 saves; keyboard taps latch like touch; the fork is a true all-clear join; first-clear route reveals do not repeat; authored direction slots keep every unlocked edge reversible; boardslides have canned approach, mount, sparks, camera move and landing; short-desktop and portrait utilities remain reachable without overlap. Removed invented cultural naming in favour of `Island 1`/`Island 2` placeholders. A manually selected native 1920×1080 full-quality diagnostic pass produced a 1920×1080 prepass and 806×454 reflection target, animated water across more than a third of frame, responsive 390×844 layout and no console errors. Finale hubs are reserved presentation nodes; boss combat remains future level work. |
| 43 | Match the temporary island silhouette to Crash 1 and the navigation language to Crash 4 | ~25 min prompt-to-playable | Automatic from `main` | 0 | `src/worldMap.ts`, campaign route guides, world-map regression and docs | Replaced the necklace of nine circular level-islets with two cohesive, data-bounded organic islands: wide irregular sand rings, lush compact interiors, shallow caustic lagoons, offshore rock stacks and clustered faceted central mountains whose route layout wraps around the mass. Rebuilt hubs as large inset green discs with thick luminous white rims, subtle low beacons and restrained collectible sockets; removed floating number plaques and boss crowns. Replaced the competing stepping-stone treatment with muted terrain ribbons beneath chunky emissive white capsule dashes, trimmed cleanly to marker rims and aligned in 3D along both walking and boardslide curves. Locked routes retain a readable cool-grey preview. Full-render 1920×1080 island-one/island-two captures, real cross-island traversal, 844×390 and 390×844 touch captures, ocean passes, map geometry regression, TypeScript and production build passed. |
| 44 | Triple the map character, reduce hub dots by 30%, and improve the island finish | ~25 min prompt-to-playable | Automatic from `main` | 0 | Map geometry/controller/UI, Player presentation, map lighting, regressions and world-map docs | The map player now renders at 3× ordinary scale and hubs at 70% diameter; path endpoints follow the smaller rims. Flushed animation-overlay and interpolation snapshots at scale handoff after browser QA caught old map scale returning during gameplay. Added camera-facing idle, rear-hub camera orbits, and portrait utility clearance. Rebuilt the island surface with smooth hills that support the trails, shaped mountain shoulders with foliage seated on the rock vertices, rounded boulders, curved palm trunks, folded gradient fronds, clustered shrubs, flowers, tropical ground leaves and flowing waterfall textures. Warm key/cool fill and gentler shadows improve the clay-like shading; the complete ocean shader retains visible caustics with calmer normals and reflections. Exact size ratios, repeated map/level scale restoration, marker clearance, desktop traversal, portrait/landscape touch frames, and the complete build script were verified. |
| 45 | Broader caustic lagoons, white shorelines, fine sand/pebbles and reusable animated tropical plants | ~25 min prompt-to-playable | Automatic from `main` | 0 | Map geometry/materials, `src/tropicalPlants.ts`, Level/editor foliage integration, plant/map regressions and docs | Expanded the bright submerged plateau to 1.95× island radius with a deeper falloff to 2.2×, retaining caustic scale 1.05. Reused the Island Hopper foam shader in one draw following all six organic coastlines in white. Layered the existing MatrixRex sand/pebble color, normal and green-channel mask at 2.7 m tiling through a vertex beach mask. Added fan palm, banana tree, sea grape, monstera and bird of paradise to the editor with scale/yaw/lean and capture-safe level data. Plant leaf colors, veins and Gouraud lighting are vertex based; monstera fenestrations are real mesh holes. The map applies the same material to its existing palms, shrubs, flowers and ground leaves. A per-level wind clock gently sways and bobs flexible leaves with matching shadow deformation and anchored stems. Instanced map groves reuse geometry; teardown releases kit resources. Full 88-command build, real leaf-hole and lifecycle checks, level-data capture, 1920×1080 map/plant gallery, desktop traversal, both touch orientations and clean browser error checks passed. |
| 46 | Soften the lagoon gradient, repair the shoreline gap, add lapping wet sand and remove round bushes | ~15 min prompt-to-playable | Automatic from `main` | 0 | `src/worldMap.ts`, coast regression and map/plant docs | Replaced the steep outer shelf lip with 65 smoothly sampled depth rings extending to 4× nominal island radius, spreading the existing ocean shader’s turquoise-to-blue transition across several dozen metres while retaining caustic size. Diagnosed nine foam samples buried by raised path-support terrain; support now fades at the submerged perimeter and foam is seated just seaward of the actual mesh/sea-level contour. Zero blocked shoreline samples remain. The same contour supplies beach-distance coordinates for a moving wet-sand front, darkening, reduced roughness and a residual damp band. Removed all rounded shrub clumps and their attached flower clusters from the ground and mountain ledges. Full 88-command build, coast clearance and wetness-clock regressions, desktop island traversal, both touch orientations and browser error checks passed. |
| 47 | Simplify the Boolie Roo launch, loading and map UI | ~10 min prompt-to-playable | Automatic from `main` | 0 | GameFlow DOM/Canvas presentation, map UI, mobile assertion retirement and docs | Changed the launch title to Boolie Roo and removed its tagline, input hint and timber/yellow container. Launch actions now use legible light text without a boxed selection background in both DOM and shader-composited rendering. Removed the map direction text, arrow buttons, Enter Level panel and region overview card. Hid the map shell during the loading vortex so it cannot appear before the destination reveal. Browser captures confirmed the clear title/loading/map screens and real New Game → map → Enter-level flow, with normal gameplay scale and no browser errors. Full 88-command build passed. |
| 48 | HUD-only bonus reward transfer and useful Modern life awards | ~25 min prompt-to-playable | Automatic from `main` | 0 | Bonus payout, Player/UI/return flow, reward/death regressions and play-mode docs | Removed the BONUS BANKED splash and bonus-label exit carryover. Already-banked fruit flies from the character to the ordinary counter with arrival sounds; rollover and direct life awards tick with the existing life-get sound. Cosmetic flights cannot credit inventory twice or consume untouched world fruit. Modern life pickups, life crates and 100 fruit now remove one death, clamped at zero; its fruit purse and face-value score remain useful across deaths. Bonus suspension restores parent deaths before applying rewards and preserves Classic reserves in Modern. Full-render Chrome checked both modes, visible flights, no title/subtitle calls, exactly 25 fruit sounds and two life sounds for a 90+25 fruit/one-life payout, and clean runtime errors. Regression tests cover rollover timing, clamping, snapshot restoration, concurrent inventory and interrupted/arriving cosmetic flights. |
| 49 | Staging secondary font, unboxed silver map hints and live tuning | ~25 min prompt-to-playable including tuning follow-up | Automatic from `main` | 0 | Map UI, reusable SVG secondary labels/CSS, settings/tuning panel, supplied font and rights notice, mobile regression and docs | Installed the supplied CCGeekSpeakTweak Bold unchanged as a replaceable staging-only secondary face, explicitly excluded from the repository license. Removed the map action bar panel, border and blur. Rebuilt the lettering as an SVG silver face over a black outline and filled glyphs swept at overlapping half-pixel intervals to form a genuinely solid extrusion. This replaces the initial CSS-layer treatment that obscured the silver face. Added a live TEXT TUNING panel for size, geometric face weight, outline, signed shadow X/Y, gradient angle/dark-band position and six colors. Clamped browser-local settings persist independently of campaign saves; Reset and clipboard/selectable JSON export are available. Input isolation prevents slider keys from moving the character. Retained keyboard hints, focus indication and clickable actions; portrait has two readable, intrinsic-width rows above touch controls. Full 88-command build, settings sanitization, and 1920×1080/844×390/390×844 Chrome checks passed, including font loading, transparent container, non-overlap, panel interaction, live updates, reload persistence, reset, copy and clean runtime errors. |
| 50 | Direct-touch map menu and hub travel | ~20 min prompt-to-playable | Automatic from `main` | 0 | World-map UI/controller, touch/input boundary, route/mobile regressions and map docs | Removed gameplay D-pad, face-button diamond, look surface, pause button and controller-symbol/key hints from the touch map only. Direct utility text buttons sit at the bottom; the selected-level card has a separate enter arrow gated by travel/unlock state. Hub taps pick projected markers and follow the unlocked graph, including multiple edges; directional taps choose a connected neighbour in the actual camera view, including off-screen islands. Gestures cannot navigate through locks or auto-enter levels. Queued routes clear on departure, map focus persists at settled hubs, and menu sections can pause/resume travel. Held gameplay inputs clear at the map boundary and normal controls return in levels. Full 88-command build, multi-hop/branch/off-screen-island route tests, full-render portrait/landscape touch entry/exit/return checks and unchanged desktop keyboard navigation passed. |
| 51 | Put game-owned UI before CRT and gate Text Tuning with M | ~30 min prompt-to-playable | Automatic from `main` | 0 | Main render routing, game-interface Canvas mirror, secondary-text Canvas painter, GameFlow cursor redraws, tuning visibility, regressions and docs | Added pre-CRT painting for map UI, touch controls, the custom menu cursor and transition curtain; removed the touch gameplay HUD exclusion while keeping native-resolution rendering. Retained semantic DOM hit targets and direct-render fallback. Text Tuning remains map-only, stays outside CRT with debug chrome, and follows M even from its summary/button focus. Cursor movement/fades request frozen-menu redraws. Gameplay glyph-sizing/alignment edits were explicitly reverted after user feedback; the existing HUD renderer is unchanged. Validation covers CRT-active map/menu/gameplay ownership, high-DPI portrait/landscape, cursor draws, live on/off handoff, M visibility and the full build suite. |
| 52 | Shared controller-family input prompts | ~35 min prompt-to-playable | Automatic from `main` | 0 | Semantic bindings/resolver, prompt DOM/Canvas renderer, Input/GameFlow/map/HUD consumers, supplied prompt assets, tests and docs | Imported 308 unchanged PNGs using Dark keyboard/mouse, Stylized PS4 colour symbols, and Default PS5/Xbox/Switch variants. Added Deck white-letter defaults with small matching SVG shoulder/trigger supplements and a missing Menu glyph. One P1 device family owns all current prompts, with controller priority while connected and keyboard/touch fallback on disconnect. Sony names/product IDs distinguish PS4/PS5; standard-mapped unknown pads fall back to Xbox, raw unknown mappings use numbered-button fallback. Options → Prompt Style handles ambiguous device IDs and persists without forcing controller art into keyboard-only play. Added native-host/glyph-provider seams. Map hints, trick-gate messages and trial retry prompts resolve shared action bindings; controls themselves are unchanged, including physical-position Nintendo labels. Images retain colour/shape, accessibility, failure fallback and pre-CRT rendering. Unit tests cover every referenced asset, families/bindings, persistence and fallback; Chrome checks cover all profiles, simulated hot-plugging, keyboard/controller priority, overrides, gameplay/CRT hints and touch exclusion. Full 89-command build passed. |
| 53 | Apply the supplied menu-text preset | ~3 min prompt-to-playable | Automatic from `main` | 0 | Secondary-text defaults, exact-preset regression and iteration log | Set the source defaults to the user's exact 13 values: size 41, weight -0.5, stroke 0.5, shadow (-1.5, 1.5), gradient angle 90 and midpoint 81, with all six supplied colours. Retained existing browser overrides and tuning behavior. Lite and full-render Chrome checks confirmed the fresh defaults and Reset restore the exact preset. No other presentation settings were changed. |
| 54 | Remove stock gameplay and unlock announcement popups | ~5 min prompt-to-playable | Automatic from `main` | 0 | Main event bindings, map popup DOM/Canvas removal, regression and docs | Removed New Path Open, checkpoint/all-boxes, time-trial/combo start, combo warning/failure/reward and automatic trick-gate banners. Left event hooks available for authored presentation and retained actual progression, checkpoint logic, HUD/counters, run-mode visuals, results and debug/error messages. Removed the map notice node, timers, styles and CRT painter rather than hiding an active popup. Lite/full Chrome checks verified silent event callbacks, retained trial/combo HUD toggles and a real clear → next-hub unlock → map-return flow with no announcement calls. Full 89-command build passed. |
| 55 | Remove map hub relics, padlocks and both fish-like decorative sets | ~5 min local implementation, clarified during ocean tuning | Automatic from `main` | 1 | Map geometry/runtime, map-only plant batch option, regressions and docs | Removed miniature collectible factories/instances and 3D padlocks from all hubs. Following confirmation of both possible fish-like decorations, removed the colourful shallow-water reef props and orange/blue bird-of-paradise blooms on the map, retaining foliage, the random placement sequence and full reusable plants elsewhere. Locked-hub colours/input guards, progress and collectible UI are unchanged. Checks cover base hub meshes only, no reward/lock/reef objects, foliage-only map groves, full reusable plants and disposal of omitted geometry. Browser checks verify removal before/after earning collectibles and preserved locked navigation. |
| 56 | Separate Map Ocean and In-Level Ocean tuning tabs | ~20 min prompt-to-playable | Automatic from `main` | 0 | Ocean profile store/preset, studio tabs, main integration, regressions and docs | Added WATER to the debug TUNER and split the existing shader controls into independent Map Ocean/In-Level Ocean tabs. Map sliders start from its authored elevated-camera preset; opening the studio no longer replaces it with Unity level defaults. Sparse parameter/debug overrides persist independently and apply even with the studio closed or after reload. Inactive-tab edits cannot affect the live ocean; each reset is profile-local and Copy JSON includes context. Legacy Unity V1 drafts migrate only to in-level settings. The existing M policy and input isolation are retained. Tests cover unchanged initial appearance, live/inactive isolation, authored inheritance, debug flags, per-tab reset, copied JSON, closed-panel/reload persistence and malformed/blocked storage. Full 90-command build passed. |
| 57 | Keep skating through low-speed obstacle impacts | ~30 min prompt-to-playable | Automatic from `main` | 0 | Player contact/pose response, pure rebound helper, collision regressions and docs | Replaced grounded low-speed frontal stops with a damped outward rebound and small lateral deflection. A 0.42-second brace/wobble keeps the board mounted and prevents held approach input or passive drag from immediately cancelling the recovery; steering away and deliberate braking remain available. Fresh box/curved-wall, rail, trick-gate and coast contacts use the response, while start-inside repairs, walking, airborne traversal and high-speed bails retain their existing paths. Added energy/heading/pose tests and real Player wall/curve/rail, held-input, near-stop, brake, respawn and high-speed cases. Lite/full Chrome verified an animated mounted rebound and unchanged high-speed bail with no runtime errors. The archived exact-frame vert replay pins its historical early-contact response solely within that fixture. |
| 58 | Skateboard map level card, 3D rewards and trial records | ~15 min prompt-to-playable | Automatic from `main` | 0 | Map presentation/UI, shared render seam, campaign trial history, regressions and docs | Replaced the old level box with the existing 3D skateboard at upper left and a 0.64-second kickflip that changes printing only while hidden. Missing rewards use flat dark silhouettes; collected rewards reuse the rotating crystal, clear/green gems and time-relic models without world halos. Cleared levels show a right-side race card with three real personal bests and the authored relic target. Added backward-compatible ranked trial history, legacy-best migration and isolated save snapshots. No hub art, level-entry or in-level trial-start behavior changed. Full 91-command build, hidden-face/queued-selection tests, record persistence/malformed-save tests, lite/full Chrome, CRT on/off, high-DPI portrait/landscape and touch navigation/entry/return passed with clean browser errors. |
| 59 | Untrap mobile map menus and label level entry Play | ~10 min prompt-to-playable | Automatic from `main` | 0 | GameFlow touch layout/close rendering, map Play DOM/Canvas, regressions and docs | Added a fixed safe-area 48px X to all map utilities and their nested confirmation screens, routed through existing Back/Cancel behavior. Touch map panels now accept swipes across their full scroll surface, align overflowing content from the top, and reset scroll on opening; desktop layout is unchanged. The X remains under CRT with dedicated opaque Canvas ink. Replaced the mobile entry arrow with a 92×48 Play button, including its filtered text and accessible level-specific label. Real touch-gutter swipes and close taps passed for Progress, Options, Save/Load and Quit in portrait/landscape, CRT on/off and lite; Play entry and return focus passed. Full 91-command build and browser error checks passed. |
| 60 | Keep map scenery rendering behind map utilities | ~5 min prompt-to-playable | Automatic from `main` | 0 | GameFlow map-background policy, isolated Level scenery tick, render loop, regressions and docs | Added a map-utility-only live render branch. Ocean/caustics, tropical plants, lapping shoreline, waterfalls and hub effects continue while navigation, route travel, player simulation and level clocks remain blocked. The ordinary gameplay pause and results paths keep their existing behavior, and static menu text reuses its cached pre-CRT texture. Touch checks in both orientations, CRT on/off and lite verified advancing ocean/plant/map clocks, unchanged player and level simulation, bounded menu uploads, functional scroll/X/Play controls and normal in-level pause. |
| 61 | Research and replace flat-looking gem/crystal rendering with efficient specular | ~35 min including historical research | Automatic from `main` | 0 | Vertex-specular material/closed shells, shared factories, research notes and regression tests | Verified Rubin's custom-specular statement in the original Crash 2 PlayStation Underground interview and separated the confirmed claim from undocumented implementation details. Added an independently authored vertex-only studio specular system with within-facet optical gradients, multiply-based lobes and untinted white flashes. Removed painted matcaps, hidden caps and translucent inner meshes; retained shapes, world halos, collection behavior and shared map/HUD use. Measured one crystal plus two gems at 3 draws/158 triangles instead of 10/188, with no specular texture reads. Closed geometry, normals, shader/clone/fade tests, multi-angle renders, real pickup/HUD, map/touch/CRT checks and the 92-command build passed. |
| 62 | Disable trial bonuses and author per-level relic benchmarks | ~15 min prompt-to-playable | Automatic from `main` | 0 | Level trial/platform lifecycle, benchmark metadata/resolver, editor field, results/map/progress consumers, validators and docs | Trial mode hides the bonus platform, removes its ground collision and rejects both proximity and direct entry; cancellation restores original ground order and preserves bonus locks. Added PROJECT → LEVEL → relic time (s), with default reset, undo/redo, validation and sparse backward-compatible level metadata. One resolved benchmark drives inclusive relic awards, result screens and map/progress targets; existing courses retain the 60-second fallback. Runtime tests cover hidden/disabled/restored bonuses and locked state, JSON capture/copy/invalid values, and equality/above-target rewards. Lite/full Chrome verified actual editor edits, reset/undo, reload, clock-triggered bonus disabling, results and map/progress consistency. Full 92-command build passed. |
| 63 | Fix grind audio becoming a hum after its first loop | ~15 min prompt-to-playable | Automatic from `main` | 0 | Shared audio loop boundaries, audio regression harness, build gate and notes | Reproduced the supplied Test Course replay's long grinds and isolated a browser full-buffer endpoint failure: the first clip plays normally, then a 128-sample render block repeats. Explicit full-duration endpoints also failed at 12/44.1/48 kHz. Managed loops now end one decoded sample before the buffer end, preserving assets, pitch, volume and one-shots. Full-render replay verification reached the same grind; five normal-rate waveform periods matched exactly and rates 0.3/1.15 no longer repeated the short block. Added sample-rate/length, source reuse, rate/gain, shutdown and one-shot regressions. All 93 build commands passed. |
| 64 | Refine gem/crystal optics against the supplied videos | ~40 min including video comparison | Automatic from `main` | 1 | Shared collectible material/optical normals, material regressions and specular notes | Compared the owner's July 11 clear-gem and violet-crystal recordings across their rotation cycles. Added separate material profiles, richer violet/lavender body shading, darker gem cuts, concentrated crown/edge flashes and a travelling long-face crystal strip reflection. Preserved physical silhouettes and animation timing. Body coverage starts at 86% and white highlights reach 100% within the same front-facing shell; whole-HUD fades still scale both, and cloned materials retain their optics. RGBA readback verified 219/255 body coverage, fully opaque white and isolated fades. Three-camera full-rotation checks, real pickup/HUD and desktop/touch map reviews with CRT on/off passed; no browser errors. Cost remains 3 draws/158 triangles for one crystal and two gems, with no new textures or passes. All 93 production build commands passed. |
| 65 | Apply updated map ocean and tune its white island outline | ~15 min prompt-to-playable | Automatic from `main` | 0 | Map ocean preset, map-only outline settings/geometry adapter, ocean store/studio, map render order, fixtures/tests and docs | Applied all fields of the supplied map preset while retaining independent in-level defaults and personal overrides. Added Map Island White Outline controls for visibility, opacity, width, shore offset, edge falloff and pulse/detail animation, with map-only JSON, persistence and reset. Width/offset edit the existing merged strip only on change, from immutable authored positions. Rails, underlays, glow and supports now draw after the outline while preserving solid-scene depth tests. Exact-preset, geometry/reset/no-idle-upload and context isolation regressions passed. Desktop/touch/lite/CRT browser checks covered live edits, hidden inactive controls, copy, reload, reset and M; real unlocked rail review and actual draw submission order verified shoreline behind rails. All 93 build commands passed. |
| 66 | Keep L2 gem/crystal inventory local to the collecting run | ~15 min including lifecycle checks | Automatic from `main` | 0 | HUD state projection, collectible regressions and map notes | Filtered crystal, box-gem and combo-gem HUD flags against the existing start-of-run saved-reward snapshot. New pickups remain in the current haul; banked rewards no longer occupy L2 slots on Retry or later visits. Kept saved ownership, duplicate-pickup suppression, results commits, map rendering and soft-respawn behavior unchanged. Unit cases cover fresh, banked, mixed and empty inventories plus baseline adoption/non-mutation. Real crystal pickup and results-button Retry checks passed in desktop/touch/lite, with CRT, a durable-slot reload and retained collected map models. All 93 build commands passed. |
| 67 | Reconstruct crystal/gem cuts, then correct face highlight sweeps | ~70 min including frame-by-frame analysis | Automatic from `main` | 1 | Shared collectible polygon factory/shader, topology/affine-coordinate regressions and specular notes | Decoded all 375 crystal-video frames and the prior gem clip; compared a full 123-frame phase sequence and the new clear-gem still. Replaced ten triangular crystal faces/centre fans with staggered quadrilateral shoulders, long pentagonal cuts and a flat foot; rebuilt the gem's table/star/bezel/girdle/pavilion cut planes and shallower profile. Geometry was reviewed before the highlight change. Flat physical normals and affine face coordinates now drive edge-to-edge broad flashes plus a separate lower-edge sliver, without a centre hotspot or diagonal crease. GPU re-triangulation differed by at most 1/255 per channel; planar/convex/manifold and no-centre-vertex checks passed. Retained 86% body/opaque white, shared map/HUD factories, pickup bounds, spin/bob and run-local inventory. Three shells cost 3 draws/178 triangles (22 + 78 + 78), with no reflection textures or extra mesh passes. Full build, gameplay/map/touch/CRT and fade tests passed. Hidden source topology/camera remain inferred; no exact original renderer claim. |
| 68 | Gate reset and checkpoint shortcuts behind visible debug menus | ~8 min prompt-to-playable | Automatic from `main` | 0 | Input reset-edge policy, main P1/P2 wiring and K/L guard, shortcut regression/build gate and map notes | Keyboard R and mapped controller Share reset now require GameFlow's live debug visibility. Disabled presses are dropped while raw held state is tracked, preventing a held hidden button or queued edge from resetting on M. K/L checkpoint skips retain their existing playtest/editor/map guards and additionally require visible debug menus. Normal menu Restart/Retry, pause/inventory input and map L Save/Load are unchanged. Tests exercise keyboard event/poll ordering, short taps, P1/P2 controller edges and held-button transitions. Lite/full Chrome verified actual reset/warp calls and the preserved normal menu actions. All 94 build commands passed. |
| 69 | Apply new map outline preset and match small-island white edges | ~10 min prompt-to-playable | Automatic from `main` | 0 | Map-only outline defaults/base width, tuner hint, ocean/map regressions and notes | Applied the exact eight supplied outline values; ocean shader parameters already matched the supplied preset. Replaced radius-scaled strip widths with a common 0.8 m base, matching the original mainland average. At width 2.9 the strip is 2.32 m on all six islands; the -1.04 m offset no longer buries the small-island brightness peak beneath sand. Preserved terrain, in-level shoreline, stored overrides and rail-above-outline ordering. Regression checks cover exact defaults, neutral/reset geometry, equal widths and terrain visibility at the bright midpoint/outer edge. Same-phase before/after screenshots, desktop/touch/lite/CRT and all 94 build commands passed. |
| 70 | Bronze/silver/gold time-trial medals with authored benchmarks | ~45 min including editor/save/render checks | Automatic from `main` | 1 | Campaign medal evaluator/save migration, per-level target metadata/editor, shared medal model, map/results/progress, validators/tests and notes | Kept the old benchmark as gold, derived silver/bronze defaults at 115%/130%, and added independently editable ordered targets with undo/reset/import/export/reload. Replaced blue relic rewards with embossed 3D medals; map sockets show the highest earned tier and the race card shows all targets. Old true relic flags migrate to gold; new bronze/silver saves survive reload without promotion and slower/no-medal retries never downgrade awards. Completion still counts one trial collectible per level. Existing result screens now show the attempt's tier and three targets in a compact layout fitting portrait/short landscape, with no new popup sequence. Unit tests cover boundaries, malformed data, upgrade/migration and model lifecycle; browser checks cover editor, all tiers/none, save/load, map/progress colours, CRT and responsive bounds. Full 95-command build passed. |
| 71 | Round medals and one map time-trial list | ~5 min prompt-to-playable | Automatic from `main` | 0 | Shared medal model, map card/semantic text, regression tests and docs | Removed ribbon and hanging loop so the circular medal fills its existing socket; missing silhouette is a filled circle. Replaced personal-best rankings and duplicate benchmarks with three gold/silver/bronze rows, showing EARNED instead of obsolete target times for won tiers. Saved records, editor targets and awards are unchanged. Tests assert circular bounds, one mesh and exact card rows for every tier. Desktop, portrait, landscape, lite and CRT browser checks passed without console errors; all 95 build commands passed. |
| 72 | Restore visible-debug K/L in normal campaign gameplay | ~5 min prompt-to-playable | Automatic from `main` | 0 | Main keyboard listener, complete-handler regression and map notes | Moved checkpoint handling ahead of the legacy playtest-only early return. Debug visibility, typing, editor, modal, map, bonus and run-mode restrictions remain; number-row level selection and capture keys stay playtest-only. Regression now invokes the complete listener with playtest both off/on instead of only its inner condition. Full-render New Game browser entry verified successful next/previous teleports and hidden-debug/paused suppression; lite passed too. All 95 build commands passed. |
| 73 | Prevent stuck mobile direction and face-button holds | ~12 min prompt-to-playable | Automatic from `main` | 0 | Touch release lifecycle, behavioral regression/build gate and release-safety notes | Added global capture-phase per-pointer release/cancel/lost-capture cleanup, zero-contact native-touch fallback, primary-touch stale-owner recovery and interruption/mode resets. Preserved independent multi-touch holds, quick taps and swipe pulses; no hold timeout or physics changes. Inspected supplied replay axes/masks but they lack raw browser events to identify a particular missed release. Real Chrome two-contact input and simulated interruptions passed portrait/landscape/full/lite checks, including neutral gameplay input and cleared highlights; all 96 build commands passed. Physical iOS verification remains outstanding. |
| 74 | Jungle Ruins Sunstone art pass | ~30 min to first playable; ~90 min including asset generation and QA | Automatic from `main` | 1 plant art-direction revision | Nine Meshy GLBs, generated soil, instanced kit/wind/shadow shaders, Jungle Ruins dressing, editor primitives, scoped framing, regression/playtest tooling and provenance | Generated original image references with built-in imagegen, then nine selected textured Meshy T2 assets (plus one discarded study). Used 150 of the 650 authorized Meshy credits; verified 676 → 526. Simplified plants to five broad leaves/fronds and clean colour planes. Replaced Jungle Ruins' old foliage, logs, temple floors/walls and pit lining; added roofed shrines, vine arches and lethal thorn beds. Model payload 3.44 MiB; plants 1,324–1,752 triangles, one 512² colour texture each. Shared templates and 32 m instance cells, matching wind in colour/depth, vine sway, canopy shade, warm soil and teal fog; small level-only camera offsets reveal the architecture. New props survive editor capture/reload and async picking; underlying movement tuning stays unchanged. Verified supported spawn/five tiers, four real-Player gap clears, complete 68 m curved-log grind, checkpoint thorn-pit respawn and finish, plus full-render browser art review. Full-render spawn measured about 60 fps (16.67 ms average) at 2560-pixel-wide internal rendering; this is the tested local device, not a universal hardware guarantee. |
| 75 | Rebuild Jungle Ruins with modular Meshy masonry and dirt trails | 4h+ additional refinement and QA, beginning 2026-09-07 15:05:39 UTC | Automatic from `main` | Modular architecture rebuild and roof UV correction | Sixteen Meshy stone modules, new canopy tree and dirt map, fitted temple/arch recipes, named editor groups, cullable near/far instances, KTX2 textures, geometry/round-trip checks and neutral assembly render | Replaced the four whole/facade GLBs with individually assembled stonework: bonded walls, bases/shafts/capitals, lintels, cornices, sloped roof blocks/hips/ridges, stairs and paving. Added two roofed spaces directly on the route; the standard pavilion contains 182 separate parts. Brown dirt covers paths, soil banks and connected missing-paver patches. Larger canopy trees and more widely spaced simple broadleaf clusters improve enclosure. Corrected roof overlapping faces, refitted hips/arch wedges, and regenerated roof UVs/textures. Added 17 Meshy generations plus one retexture: 265 additional credits, 415/650 cumulative, balance 261. The 22 shipped models total 19.98 MiB with 2k KTX2 albedo, JPEG fallback and real far geometry; measured kit texture estimate 135 MiB. Preserved movement tuning and traversal support. Full build, real-Player gap/log/checkpoint/pit/finish tests, actual-geometry coverage and editor capture/picking checks passed. Full-render spawn measured about 60 fps at 2560×1252; the temple interior measured about 56 fps before moving stone LOD from 48m to 32m. Final live browser/editor and interior timing verification awaits the Mac being unlocked; this limitation is recorded in the review JSON. |
| 76 | Grass-edged worn path, lighter earth and black Jungle pits | ~40 min prompt-to-playable including isolated validation | Automatic from `main` | 1 path colour/blend refinement | Jungle ground shader/shoulder attributes, deep-scene fade, optional Meshy editor catalog and asset tests | Kept the approved brick GLBs, temple assembly recipes and planting layout. Added a curved, irregular grass transition around a lighter sandy dirt centre for crate contrast. Hidden thorn renderings retain all 40 original death volumes; pit walls and floor fade to black after fog. Added five previously generated clay/aged brick and paving variants as separate editor assets, with compressed textures and near/far geometry; none replace the level's bricks. Added no generation spend: cumulative Meshy accounting remains 465/650. Validated original module preservation, real asset loads, curved grass attributes/shaders, hidden hazards, editor reconstruction, four gap clears, the ravine grind, checkpoint respawn and finish. Release was isolated from concurrent editor work. |
| 77 | Stress-test and harden the public level editor | ~1 h prompt-to-playable audit and local QA; isolated release verification followed | Automatic from `main` | Browser and regression-driven fixes | Editor/runtime/format, native scene owners, import/sync boundaries, regression gates and format guide | Added strict bounded JSON parsing before allocation/build, whole-pack validation, safe storage recovery, visible rejection/save feedback, search and bounded undo. Fixed cross-level group wiring, atomic batches, canceled scrubs, path/terrain rotation and knot topology, owner return/support anchors, stale inspector values, HUD/index alignment and lost curved-rail motion. Native map/bonus/traffic/coast/tumble/triangle/ocean data preserves gameplay systems and irregular ground through capture/edit/import. Security suite covers 91 adversarial/source cases; 16 editor scenarios plus environment and actual Level collision/transform tests pass. Browser verified shared-wrapper import, literal HTML-looking labels, rejected prototype/deep/oversized files without replacement, environment add/remove, numeric undo/redo, linked portal movement, checkpoint warp, moved custom-map launch and full rendering with no console errors. Full build passed; release is checked from an isolated checkout excluding unrelated character work. Strict legacy object equality remains diagnostic: some hand-coded material effects and visual-only scenery lack full conversion parity, documented in LEVEL_FILE_FORMAT.md. |
| 78 | Close editor expansion, transaction and shoreline reliability gaps | ~20 min prompt-to-playable plus full release verification | Automatic from `main` | Evidence-driven follow-up | Validator/build budgets, wall collision, registry transactions, ocean v2 migration, responsive editor docks and regression tests | Closed billion-part masonry generation from small JSON; bounded complete long/thin/diagonal wall collision; revalidated canonical migrations and wrapper titles. Preserved undo/redo and selections on library-cap failures while keeping quota-limited session drafts exportable. Protected locked wiring, fixed old-target gesture cancellation and reentrant pointer-capture release. Ocean migration preserves old footprints while aligning edits/conversions with world coordinates and retaining upward water faces/native sampling. Compact docks, unobstructed tools and a fixed TEST footer work in portrait/landscape; 118 security cases, 144 assembly comparisons, 18 editor UX cases, 15 transaction cases, environment/native full/lite tests and full build are release gates. Ongoing work remains recorded in EDITOR_RELIABILITY_AUDIT.md: terrain-support triangle cost, legacy visual capture, multi-touch and large-library performance. |
| 79 | Consistent map axes, readable side paths and canonical level roster | ~45 min prompt-to-playable including layout/touch iterations | Automatic from `main` | 0 | Campaign graph/identities, map geometry/camera/touch routing, registry/migration, tests and map notes | Main progression and multi-level side routes use Right/Left; reciprocal Up/Down junctions connect paths. Island 1 has the optional Slipstream → Codex Switchback upper loop; Island 2 has the compact Chimeworks lower branch. Promoted codex-lab and astra-chimeworks with stable new campaign keys, saves, collectibles, medals and same-hub returns. Removed Jungle Cliff's runtime source/registry and dedicated test; stale editor copies remain exportable but not playable. Fitted islands to irregular coast outlines, moved mountains/islets clear of routes, widened ocean coverage and framed portrait riders below cards. Shortened the lower branch after actual landscape taps hit the nearby Options target. Existing nine-hub editable maps preserve identities/custom positions; unchanged defaults migrate to the new layout. Full route matrices, actual touch entry/returns/off-screen next, canonical launch/quit, screen-axis/framing, saves/migration and all 99 build commands passed. |
| 80 | Enclose Jungle Ruins and seal accidental boundary exits | ~60 min prompt-to-playable and focused boundary review | Automatic from `main` | 1 backdrop colour/placement refinement | Low-detail rock/canopy backdrop, closed source-owned perimeter, containment collision/validation/editor control, regression probes and docs | Preserved the approved foreground planting, brick modules, lighter worn trail and black pits. Added 72 broad cliff placements and 36 enlarged far-mesh canopies behind the outer row, plus rock faces beyond both ends; 25,416 additional triangles and no extra texture resources or Meshy credits. The 2.4 m perimeter follows floor widths and their hard corners, spans Y=-18..64 and caps the start and finish. Containment contacts resolve adjoining faces together after ordinary collisions and cannot be wallridden or grabbed; normal walls, rails and movement tuning remain unchanged. Validated 112 actual-Player walking, air, fast-skate and corner/end attempts, serialization/rebuild, four intended gap jumps, the ravine grind, checkpoint respawn and finish. |
| 81 | Expand and refine the island-map landscape while preserving ocean treatment | ~85 min including modular generation, style correction and QA | Automatic from `main` | 1 foliage rejection | Procedural map topography, map-only GLB kit and terrain texture, existing foliage instancing/LOD, scene loading/disposal, tests and asset ledger | First-island dry width is 201.56 m / 2.75 normal 16:9 screen widths. Added a caldera, ridged flanks, valleys, terraces and cliff surface detail to both islands; moved Island 2 clear of the enlarged coast. Three independently generated/textured Meshy rock modules are 6,494/6,709/6,630 triangles, each with a real far mesh and KTX2/JPEG fallback. Rejected the noisy rainforest tree after user feedback, removed it from public assets/catalog and reused established clean canopy/palm assets with slope-aware roots and restrained density. Spent 60/300 credits: 45 selected rocks + 15 rejected tree. Ocean shader/presets/outline values remain unchanged; only lateral water coverage expanded to hide its old edge. Preserved canonical identities, Left/Right paths, vertical branches, collectibles and same-hub returns; upgraded unchanged prior default map captures without overwriting authored positions. Screen-width, full/touch views, route clearance, source/asset budgets, disposal and all 102 build commands passed. Local Chrome measured about 60 fps at 2560×1440 and 780×1688; this is not a physical iOS benchmark. |
| 82 | Bound support probes, protect pointer ownership and preserve legacy scenery | ~90 min to validated playable fixes; deployment QA followed | Automatic from `main` | Audit-driven reliability/performance pass | Ground acceleration/budgets, immutable prepared registry transactions, pointer lifecycle, native scenery capture/materials/groups, tests and audit notes | Added early BVHs with owned-tree transfer/cleanup, bounded overlap and actual ray work, and exact pre-save support-build checks; kept native Beachfront/Island support behavior. Removed repeated whole-library and active-draft validation through immutable snapshots and opaque prepared tokens, preserving quota/rejection/history semantics and correcting copy/export names. Candidate-first level switching preserves the previous world and state on failed construction; ten actual-handler cases verify failure and successful native VFX. Fixed multi-pointer ownership, touch-camera handoff, scrub/capture/blur/resize/view transitions. Preserved Descent paint, barriers, mountains, islands and 214 pines within existing file limits, linking wall/rail visuals to physical components; respected authored terrain materials under jungle lighting. 20 event cases and 6 synthetic browser cases pass with saved storage unchanged; 294 scenery checks and support/registry/source regressions pass. Full lite/full browser and clean build/deployment remain release gates. Broader legacy themes/source-only behavior and physical-device/render-cost checks remain tracked in EDITOR_RELIABILITY_AUDIT.md. |
| 83 | Natural floating-rock Nightworks and flexible braided ropes | ~45 min to the clean playable art pass; collision/editor/release validation followed | Automatic from `main` | 2 top-surface fitting corrections | Seven Meshy assets, source-owned Nightworks, matched rock collision, global rope geometry/curve, tests and editor ferry phase | Used 105/1,000 authorized credits; seven models total 6.2 MiB with real LODs and compressed/fallback textures. Replaced all 57 islands and five travelling grind ridges with warm flat stone rims and pointed slate undersides; added floating rope anchors, supported beacon crags and distant arches. Kept route/cycle timing, joined two touching slab pairs, preserved checkpoints and summit gate. Collision uses the same fitted geometry as the render, with swept sides/undersides and moving ledge support. Global ropes have helical hemp shape, fibre shading, secondary bend/sway, matching grip/velocity sampling, and rebaked grind-rope sag. Validated 456 side/corner sweeps, 453 real-player catches plus three supported joins, 1,764 visual/collision rays, moving mantle, phase disappearance/late asset readiness, rope curve/IK/board transfer, JSON round trip, void death, summit finish and landing-marker replay. Integrated the latest editor fixes and passed the full build suite. Full-render Chrome averaged ~16.67 ms at 2966×1440; this is a desktop observation. |
| 84 | Preserve editable atmosphere/materials/motion and stop ocean texture leaks | ~55 min to playable fixes and local verification; release QA followed | Automatic from `main` | Evidence-driven editor follow-up | Bounded atmosphere resolver/controls, portable Sky exports/copies, trusted shoreline material, signed travel and rope clocks, GPU lifetime fix, runtime/browser gates and audit reports | Native fog/light/backdrop values survive new IDs; legacy published Sky copies retain the 5–24 m envelope, with explicit fog choices respected. Beachfront sand preserves Standard normal/AO maps and metric UVs, Descent oil stays untextured, and nine surface types retain editable glow. All allowed sand patches and styled meshes share three maps per Level, closing a 768-texture allocation path from small JSON. Rotations preserve platform/rail/ferry direction and timing, including independent native rope ferry phase and length-dependent speed. Real-browser Descent rebuilds exposed six leaked ocean textures per operation; direct owned shader samplers hold 13–14 live textures and return to one shared texture after cleanup. Nine edit/undo/redo operations pass in lite/full geometry modes; full sand comparison and five rebuild cycles have stable resources. Source/default renderer baselines, hostile-field/transaction checks, actual motion/collision/material regressions and the full build remain release gates. Merged the concurrent floating-rock Nightworks and flexible-rope update, preserving its new atmosphere and adding signed rock motion, covariant ridge alignment and rock transform controls. Existing thorn, car-control and native lens findings are documented for a future request; goal pursuit closes after release at the user’s request. |
| 85 | Stop Nightworks camera/input handoffs hijacking runs | ~5 min prompt-to-playable; release checks followed | Automatic from `main` | 1 | Nightworks camera data and regression coverage | Replaced the bent route-following spine and competing side-scroll input zones with one stable negative-Z camera spine. Switchbacks remain side-on; the player supplies the turn and held input keeps the same world direction across every former handoff. Added 500 grounded/airborne camera samples and 20 actual-player held-input crossing checks. Rock geometry, obstacle cycles and movement tuning are unchanged. |
| 86 | Forward-looking camera for the Nightworks rail-timing crossing | ~15 min prompt-to-playable; release checks followed | Automatic from `main` | 1 | Authored fixed-view camnode volume, shared camera sampling, held-input frame, editor controls and regression checks | Added one feathered forward view covering the full antiphase sweep of the long two-rail crossing. Kept the default fixed spine elsewhere and avoided travel-zone axis remaps. Both cameras sample the same bounded heading; continuous movement keeps its input frame through the blend, while released/changed input follows the current view. Verified every rail sweep/height sample, unrelated section headings, 90 camera-blend input samples, real-player held running/fresh forward control, prior collision/rope coverage and camera-volume serialization. |
| 87 | Align editable thorns, retire cars and remove Beachfront lens special case | ~40 min for fixes and local checks; deployment verification followed | Automatic from `main` | Scoped follow-up | Thorn defaults/inspector/handles/transforms, retired-vehicle migration/runtime removal, both gameplay camera paths and regression tests | Thorn and pit selections rotate together; dimensions, yaw, glow and optional seed are editable with correct base-anchored resize and pure defaults. Verified actual vertices, lethal membership, pulse phase, repeated turns, scale/move and saved undo/redo. Removed moving and parked cars, their editor options and runtime/contact logic; old local/shared files safely drop vehicles and obsolete traffic metadata while retaining the road. Removed the six-degree Beachfront FOV offset from both cameras without changing global tuning values. No new responsiveness optimization was attempted because prior proven wins were already applied; touch/pen remains ultra-low priority. Full build and real-browser lite/full smoke checks are release gates. |
| 88 | Give fatal pit/enemy hits an extra 1.5 seconds on screen | ~18 min prompt-to-playable including browser verification; full release checks followed | Automatic from `main` | 0 | Death presentation timing, corpse motion, fade delay and regression coverage | Added a 1.5-second visible beat before the existing 0.7-second respawn interval. Fatal hits retain momentum and use the existing ragdoll pose and ground-bounce response, with damped wall rebounds; corpse motion cannot collect rewards, attack or charge another life. Kept immediate death callbacks and delayed only the curtain. Tests cover timing, visible falling, wall rebound, life accounting, bonus/Game Over and respawn; normalized the legacy ledge/landing replays' pre-change death timing to preserve its fixed input timestamps. Lite/full Chrome verified visible corpse, delayed blackout and successful respawn without console errors. |
| 89 | Restore pre-enhancement Jungle camera and extend shadow draw distance | ~8 min prompt-to-playable; full release verification followed | Automatic from `main` | 0 | Both gameplay camera paths, sun shadow coverage/depth/resolution, camera/shadow regression checks | Compared with pre-enhancement commit 7fa7f8a: shared defaults already matched (height 5.1, distance 5.05, pitch 25.35°, FOV 49°). Removed Jungle-only +1.8 m height, +2 m distance, -4.5° tilt and +5° FOV offsets in both camera paths. Expanded level shadow width from 46 to 192 m and map width from 116 to 232 m; doubled map resolution to 4096². Moved the sun three times farther along the same direction and extended its far plane to 600 m, adjusting depth bias to retain its prior world-space scale. Tests project distant/tall casters through all four lighting directions at origin and translated focuses, alongside existing camera controls/lens/replay checks. Full-render Chrome verified restored lens/pitch and real 4096 shadow allocation at spawn and the next checkpoint, averaging 16.67 ms over 119 frames at 1440×900 with no console errors (desktop observation). |
| 90 | Remove Unity-derived unlit skateboard treatment | ~12 min prompt-to-playable; full release checks followed | Automatic from `main` | 0 | Shared skateboard materials, map-card print, discarded/fractured board shadows, regression tests and port notes | Replaced deck custom unlit output and Basic plywood/wheel/truck materials with scene-lit Standard materials. Kept artwork UV crop and procedural wear as albedo inputs to Three's lighting/fog/shadow pipeline, including native instancing. Enabled board cast/receive shadows and preserved those flags on fractured and batched resting pieces; exposed split wood is lit too. The map's printed skateboard face also uses a lit material. Preserved geometry, dimensions, textures, settings and movement. Tests cover lit material types, wear/crop injection, standard instancing/light chunks, shadows and existing board lifecycle/ownership. Lite/full Chrome verified the mounted gameplay board, imported trucks and lit map-card print with no console or shader errors. Full build and deployment verification are release gates. |
| 91 | Keep the map time-trial top three visible after medal awards | ~6 min prompt-to-playable; full release checks followed | Automatic from `main` | 0 | Map race-card painting, accessible record text and regression checks | Restored a persistent 1st/2nd/3rd personal-record list above a compact medal-progress section. All three saved times remain visible after bronze, silver or gold; missing records use the existing clock placeholder, while unearned medal targets and earned status remain available below. Accessible text mirrors both sections. Tests exercise every medal state with zero, one and three records, plus existing hidden-face swaps and renderer routing. Full-render desktop/390px portrait checks show three distinct records with all medals earned, without console errors. |
| 92 | Add dedicated skateboard appearance tuning to the main menu | ~18 min prompt-to-playable including focus handoff and browser checks; isolated release validation followed | Automatic from `main` | 0 | Main-menu/Options entry, existing board panel, modal focus/input ownership, desktop tuner access and regression coverage | Added SKATEBOARD TUNING to the main menu and Options, opening the existing saved board controls in a dedicated centred appearance panel without exposing developer tools. Retained shape/curve/wheel/truck/artwork/wear/plywood controls and reset/import/export/full-lab access. The panel owns focus, traps Tab, makes the covered menu inert and closes with X/Escape/controller Back; held controller input cannot activate the menu when it closes. Added BOARD to desktop presentation tools too. Browser checks cover debug-hidden opening, numeric edits, persistence after reload, reset, close/focus restoration and 390px portrait layout without errors. Validation uses an isolated checkout to exclude concurrent unfinished map work. |
| 93 | Replace perforated map art with solid clay forms; remove waterfalls | ~100 min including image/Meshy work, mesh repair and isolated release checks | Automatic from `main` | 2: Meshy exposed-face modules; remove waterfalls rather than rebuild | Closed foliage factory, repaired cliff modules, stepped landforms, asset topology gates, loading/disposal and asset notes | Removed imported map canopy/palm meshes, old perforated rocks, the pasted cliff texture, waterfall strips and their pools. Added 3–9 thick closed leaves per plant with shape-preserving lower detail, opaque front-face rendering and anchored wind. Generated two clay-style exposed cliff pieces in Meshy, then sealed/closed unintended tunnels, cleaned fragments, flattened embedding backs and baked restrained vertex colours. Exported near/far buffers independently pass manifold, duplicate/degenerate face, winding, single-component, genus-zero and positive-volume checks; near meshes are 6,500/6,494 triangles. New pieces share the existing instancing/LOD pipeline and sit partly inside terrain; nearby foliage avoids their footprints. Defined broad terraced cliff planes and removed the ungraded centre-vertex spike. Ocean, crater lake and other levels' scenery remain unchanged. This revision used 30 Meshy credits, 90/300 cumulative including rejected work. Isolated production build passed 113 checks; desktop/portrait/landscape renders, full navigation, touch junctions and same-hub returns passed without runtime errors. |
| 94 | Jungle Cup: Island 1 skate-competition boss | ~35 min to first playable event (estimate); park refinement, campaign integration and release checks followed | Automatic from `main` | Park/camera and campaign migration refinements | Source-owned jungle skatepark, three-run model/UI, judge/rival tuning, player event hooks, unique cup/save/progress, map routing and regression coverage | Added a dedicated boss after Nightworks with three 60-second runs, sequential three-judge scoring, nonlinear bail penalties and best-two totals. Ordinary skaters cap at 94; the rival stays in a varying elite band, guaranteeing 1st/2nd without rewriting revealed marks. Only an overall player win awards the one-off cup and opens Island 2. The 82×110 m Jungle Ruins park has a terrace drop-in, funbox, manual pads, spine transfer, halfpipe, opposing quarters and connected rails, with matching masonry bounds and a new editable cup prop. Preserved banked points/reserve lives across competition deaths, counted each wipeout once, settled landed buzzer combos, removed unrelated bonus/crystal/trial spawns, and kept normal movement tuning. Tests cover judging/state/save invariants and real-Player geometry/rail/death/gate behavior; browser checks cover loss/retry, win/unique trophy, Progress, pause/restart, judge reveals and responsive final standings. |
| 95 | Separate map-card skateboard tuning inside Skateboard Lab | ~15 min to working preview; validation and release checks followed | Automatic from `main` | Corrected interpretation of the prior menu request | Independent settings profile, lab tabs and exact map-card preview, live map binding, fitted print/icons, main-menu rollback and tests | Added GAMEPLAY BOARD / MAP UI BOARD tabs inside the existing lab. Map shape/art/wear/colours save to a distinct fork-owned key, retain the prior 0.34-width/0.6-wear defaults, and update the actual map card and other open map tabs. Printing and collectible icons stay uniformly fitted; a Flip button previews the underside. Added a split preview/control layout for narrow screens. Reset/import/export target only the selected profile, including asynchronous imports, and imported truck templates are cached per model URL. Removed the earlier main-menu/Options entry and its modal plumbing. Regression and real-browser checks cover independent edits, resets, legacy imports, reloads, cross-tab updates, preview geometry, flipping and portrait layout. |
| 96 | Jungle Cup vert, rider controls and chase-camera rebuild | ~11 min to first playable; ~100 min through final polish/full validation, including ~46 min waiting on locked emulator access | Automatic from `main` | 0 scope approvals | Park data/profile, swept vert contacts, rider-relative control/air rules, camera/pose, competition hints and regression/browser harness | Expanded the foundation from 82×110 m to 120×172 m (2.29×), with one 90-degree perimeter bowl, round corners, five-metre decks, a full-height safety shell and continuous four-way street islands. Removed crates/checkpoint, gate, ceremony dais and in-level trophy. Starts mounted; left/right steer the rider, down brakes on the board, and release restores control without a walking lock. Removed the 1.8 m/s inward vert shove, added face-normal riding and swept landing contact, retained coping momentum and levelled the apex pose. The chase rig follows jump height, handles bounded 180s, opens the return line and targets the tilted torso to clear the coping. Editor copies retain the profile without recreating gates/activators. Full build passed. Focused checks cover 24 same-plane returns across four sides/four corners and three speeds, 24 terminal-speed contacts, the supplied 3,117-frame replay, 19,200 mixed-input frames without floor escape/transition lock, mounted stop/turn/restart, camera-input independence, obstruction/framing and 30/60/120 Hz heading updates. Lite/full browser checks cover apex/re-entry and a scored carve/vert/manual-island run with no console errors. Earlier THPS audit commits were reviewed; OpenEmu's visible reference/settings were inspected, but its gameplay did not accept synthetic taps, so no controlled THPS comparison is claimed. **Rejected in user playtesting for vert/camera feel; superseded by iteration 97.** |
| 97 | Correct Jungle Cup against actual THUG physics/camera source; final-combo overtime and always skate | ~3 h 15 min cumulative from the original goal through corrected browser verification, including the rejected first pass and source audit; final release checks followed | Automatic from `main` | User rejected iteration 96 and added overtime/automatic remount requirements | Dedicated park calibration and full surface velocity, real vertical coping, tracked ballistic vert, surface-frame camera, landing/trick judgement, event lifecycle and regression evidence | Read the pinned THUG core physics, camera and original script parameters, plus Claude's relevant upstream descriptions/diffs; recorded provenance, numeric conversions and adaptations in `docs/THUG_SKATING_REFERENCE.md`. Replaced flat-heading projection, early crest/free pop, steepness pump, apex upright pose and yaw-return camera. The bowl uses a 3.6 m radius plus 0.8 m vertical top; natural edge exits carry their actual velocity, charged releases add 2.54–6.985 m/s, and locked vert uses constant 31.1727 m/s² gravity with small curved-coping feelers. Neutral kicks, crouching, braking and rotation use the reference calibration. Full orientation camera uses wall-relative screen-up through vert and faster touchdown recovery. Automatic angled turns do not score; completed manual 360s land and off-axis 90s bail. Overtime remains playable at 0:00 until the active combo resolves, including normal links; bail recovery returns the board without input. Checks cover 24 vert returns, 24 terminal contacts, source arc/impulse/frame invariants, three actual spin landings, 19,200 stress frames, all 3,117 replay frames, 100 simulated overtime minutes, editor round trips and remounting. Lite/full browser review covered apex/drop-in, carving, replay, a final 510-point bank into 1,744 total, final bail, automatic remount and clean console. Up transfer additionally waits for the foot feeler to clear the coping. All repository checks, TypeScript and the production bundle passed, with the build resumed after replacing a brittle source-length assertion with a simulation-order check. OpenEmu retry accepted Cross, but focused continuous gameplay remained unavailable; no controlled THPS2 timing comparison is claimed. |
| 98 | Preserve rail follow camera and eliminate upper-lip bail soft locks | ~10 min to verified fixes; additional handoff, browser and release checks followed | Automatic from `main` | User approved vert feel/camera; fixes limited to grinding and recovery | Signed rail camera heading/support, immediate flight-state retirement, coping momentum handoff, physical-slope recovery gates and targeted regressions | Fixed the camera reading a stale pre-grind heading by following the current signed rail tangent through catches, corners and exits. A catch retires the old vert view immediately; incoming park vert coping velocity is captured before cleanup, preventing forced direction reversal/minimum-speed resets. Flat decks sharing a vert mesh no longer erase the bail heading or accelerate a stationary ragdoll. The transition settling-speed check now only gates entry to recovery, so the roll-up cannot cancel itself as it gains run-out speed; loss of real support still interrupts it. Added 16 actual rail catches, 553 grind frames/14 exits, both coping-velocity signs and zero-speed fallback, plus 49 neutral-input bail cases across every deck/corner, coping seams, airborne landings, standable banks and an actual upper-vert bail. All recover and remount within 1.73 s without heading loss, duplicate bails or floor escape. Existing approved vert/camera regression and full build passed; lite/full browser checks confirmed deck, bank and corner remounts and centred rail following without console errors. No park tuning, ramp geometry or chase-rig constants changed. |
| 99 | Give Jungle Cup varied THPS competition sessions and restore the sky | ~30 min to first playable geometry (estimate); validation and an overnight locked-host pause preceded final visual review | Automatic from `main` | Host unlocked for final browser review | Source park layout, outside-bank authoring, sky distance/haze, flush-coping identity, cold-run recovery state and route regressions | Studied the original THPS2 manual and illustrated Marseille/Skate Street guide sections; documented sources and route roles in `docs/JUNGLE_CUP_LAYOUT.md`. Kept the approved 4.4 m true-vert perimeter and skating/camera tuning. Added a 2.08 m banked pocket bowl with a low divider, a four-step plaza and handrail, curved manual terrace, round hip/crescent rail, three rounded rollers and two banked connecting channels; 13 grind lines now serve the park. Added `vertramp.outerBank` with validation/editor/scaling support so the pocket bowl has one continuous rollable outside surface. Increased authored draw distance from 290 to 520 m to contain the 370 m painted sky dome and lightened distant haze. Final browser review exposed flush coping being treated as a street barrier; generated coping now has explicit runtime identity, while real bars still trip. Restart also clears stale vert landing grace, which had masked the short entry test. The full cold-run bowl crossing, four pocket airs, channel traversal, rhythm jump/grind route, 14 new rail catches, existing skating/camera/recovery/replay suites and full build pass. Lite/full browser review confirmed clean pocket-to-perimeter, stair, rhythm and hip lines, visible sky and no console errors. |
| 100 | Integrate Jungle Cup menus below CRT and keep score/avatar visible | ~23 min through local browser verification; release verification followed | Automatic from `main` | Preserve the avatar because it owns SPECIAL | Competition selection/input, cached Canvas ink in the shared interface pass, competition HUD mode, responsive timer/actions and regression/browser coverage | Added visible selection, pressed feedback and matching focus for four-way controller, keyboard and pointer navigation, with held-input and disabled-action guards. Competition intro/countdown/clock/judges/standings/podium now use the existing pre-CRT interface pass; semantic DOM retains accessibility and hit targets, with normal DOM fallback. Cached ink supports actual font layout, native portraits/trophy, prompt glyphs, scroll clipping and inactive canvas release. Removed the strip’s duplicate points display; the original score plate and avatar/SPECIAL remain visible for the whole run and unlimited overtime, independent of inventory toggles. Portrait checks exposed a timer/avatar overlap; a compact timer clears the avatar at 390 and 320 px, and short-screen cards keep their actions visible while scrolling. Real-browser CRT checks covered controller highlight/held confirm, judge cards, winning standings, score/SPECIAL and overtime; lite fallback covered losing standings and keyboard selection, without console errors. Focused interaction/HUD regressions, all repository build checks, TypeScript and the final production bundle passed. |
| 101 | Audit trick controls/animation and rebalance repeat/hold scoring | ~73 min through full automated verification; deployment followed | Automatic from `main` | User explicitly waived visual review because the Mac remained locked and requested publication | Shared trick loadout/guide, independent air-trick buffers, deck/body animation, run history, held-score accounting and tests | Read pinned THUG score history, air/grind scripts and character loadouts; verified the cited score source byte-for-byte and recorded the comparison and implementation limits in `docs/TRICK_SYSTEM_AUDIT.md`. Added eight directional flips and eight latched grabs with discoverable controls/points, preserving existing gate IDs and updating their hints. Park Square bypasses attack cooldown/air impulses; flip/grab transitions buffer short inputs, used ordinary chords cannot accidentally seed specials, and late park flips bail while same-tick catches score once. Corrected half-shoves, retained deck heading, moved flip/inversion after foot seating, added foot clearance/orbit and a hip-centred McTwist. Rebalanced flip/grab/grind/special values, applied 100/75/50/25/10% decay across landed combos and hold points, retained fractional accrual, softened long holds and reduced stationary grind pay. Bailed attempts discard pending usage; soft respawn and bonus returns retain landed history; new runs reset it. All 32 flip/wall combinations, buffered chains, specials, scoring/history/rounding tests, the existing competition physics/camera/recovery/replay suites, full build, TypeScript and final bundle pass. Browser/visual review was skipped only for this release at the user’s explicit request; exact grab hand contact remains an animation limitation rather than a claimed fix. |
| 102 | Rename game menu branding to BONEMAN | ~5 min through title/font checks; release verification followed | Automatic from `main` | 0 | Shared menu logo, browser/install titles and manifest | Replaced the Boolie Roo main-menu wordmark with one BONEMAN title using the existing shared DOM/pre-CRT logo renderer. Sized the longer word against the real Roo font metrics at 320, 390, 836 and 1440 px. Updated the main and game-review browser titles, Apple installed-app titles, and manifest name/short name. Existing menu/compositor interaction checks pass; full build and deployment remain the handoff gates. |
| 103 | Fit pickup/smash bounds to the live character and magnet fruit before HUD flight | ~3 h to first full-render review (estimate, including locked-host pause); extended regression/release checks followed | Automatic from `main` | Host unlocked for browser review | Posed rider bounds, crate contact, world fruit attraction/ownership, Character Lab diagnostics and regression/browser fixtures | Pickups and destructible crate attacks now include current height, active head proportions, visible parts, morphs and skinning, without the old short attack cap. Solid support retains the authored movement collider. Fruit within 1.75 m accelerates toward the moving actor and only switches to the existing HUD flight on contact; accounting still occurs at HUD arrival. Death/reset/run modes and two-player ownership preserve unearned fruit without duplicate rewards. Full-render and lite browser checks covered enlarged/small bounds, crown pickup, keyboard crate spinning, visible magnet-to-contact-to-HUD movement and a single counter increment. Current interaction and skating tests cover contact boundaries and lifecycle; historical input-only vert/ledge/locomotion takes pin their original crate envelopes to preserve their later route assertions. All repository check groups, TypeScript and the production bundle passed after the contact fixes. See `docs/CHARACTER_INTERACTIONS.md`. |
| 104 | Generate the first procedural milk pickup asset family | ~15 min to in-game visual review (estimate); full regression and deployment followed | Automatic from `main` | Bottle HUD/fill and carton assets explicitly deferred by user | Six shared milk surfaces, opaque wet shader/deformation, pickup/flight integration, interactive gallery and asset checks | Replaced world pickup and collection-flight art with Pearl, Pear, Twin, Drop, Puddle and Cloud variants inside the existing 0.7 m envelope. A texture-free opaque shader gives cool soft body shading, cream-white light, wet highlights and a restrained phase-varied stretch with corrected normals and matching directional shadow depth. Native-to-magnet-to-HUD handoffs preserve the selected geometry; crate clumps cycle through variants. Existing HUD art, boxes, accounting and sounds remain for the later asset pass. The gallery supports rotation and dark/light backgrounds. Browser checks covered gallery contrast plus actual pickup/contact/HUD motion in lite and full rendering without console errors. Asset tests cover closed surfaces, smooth normals, size, shared ownership and material clones. Full `npm run build`, including the updated milk handoff regression and production bundle, passed. See `docs/MILK_PICKUPS.md`. |
| 105 | Restore grabless spin feedback and reshape milk into droplets | ~20 min to visual verification (estimate, including unlock troubleshooting); regression and release checks followed | Automatic from `main` | Host unlocked after the computer-use tool reported its automatic unlock had failed | Bare-rotation combo preview/classification, park vert minimum, tapered milk surfaces and regression/review fixtures | Degree-only spins now show pending and landed combo plates, trigger normal cash-in feedback and own final-combo overtime. Park vert pays from the first player-controlled 180 while its automatic drop-in turn remains unscored; legacy non-park minimums remain. A quarter-turn cannot preview a landable 180. Real-input tests cover ollie 180s and vert 180/360s both ways, exact single awards/multipliers, overtime and bail/no-spin guards. Replaced egg-like milk ellipsoids with rounded reservoirs, narrow necks and pulled/curved tips across all six profiles, retaining the shader and pickup size. Browser review confirmed the new droplet shapes, visible 80 ×1 ollie feedback, a landed 160 ×1 vert 360 and its 160-point cash-in, with no console errors. Full build and the final spin-label/quarter-turn and droplet-tip regressions passed. |

| 106 | Expose live milk attraction distance | ~5 min through runtime and tuner review (estimate); build/release checks followed | Automatic from `main` | 0 | Shared tuning metadata, activation gates and contact regression | Added TUNER → MILK → Magnet distance (m), default 1.75 m, range 0–8 m in 0.05 m steps. Placed orbs, crate drops and peer-player drops read the current value. Zero keeps direct contact, including a peer drop whose centre sits outside the character while its body overlaps. Existing attracted milk completes its trip. The browser slider changed to 1.8 and back to 1.75; runtime tests verify live activation changes and zero-range peer contact. Corrected the historical defaults expectation for the added setting; that focused check and the lightweight build pass. |

| 107 | Make the full test suite explicitly opt-in | ~3 min through focused verification (estimate) | Automatic from `main` | User requested full-suite runs only when explicitly asked | Build scripts, deployment workflow and persistent project instructions | `npm run build` now performs TypeScript checking and production bundling only. The previous aggregate test chain is retained as `npm run check:all`. Normal push deployments skip it; manual workflow dispatch offers a default-off full-suite input. AGENTS.md records focused checks by default and explicit authorization for full-suite runs. Cancelled the previously running CI suite. Focused defaults verification and the lightweight build passed. |
| 108 | Animate milk as liquid and unify the idle silhouette | ~25 min to game motion review (estimate); focused checks/release followed | Automatic from `main` | User supplied an upright rounded-drop reference and rejected separate/slanted base shapes | One shared teardrop, independent morph/spring animation, measured world/HUD velocity, handoff/reset integration and gallery modes | Replaced the six base meshes with one symmetric, soft-tipped drop. Timing offsets create idle variety. Shared position/normal morph targets add near-volume-preserving extension/compression, trailing bends and ripples driven by release and travel. Native and pooled idle forms remain upright; world-to-HUD transitions copy state and rebase position tracking. Crate release retains an upright orientation through ascent and descent, following the final user correction. Full-render review showed vertical crate rebound, a visibly stretched HUD flight and one counter increment; focused geometry/motion/30–60–120 Hz and pickup lifecycle checks pass, along with the lightweight build. No full suite was run. |
| 109 | Replace the rotating fruit HUD with the supplied filling milk bottle | ~20 min through image/HUD review (estimate) | Automatic from `main` | User requested an animated drink-down at rollover, with the numeric count unaffected | 101 supplied PNGs, bounded image cache, one texture per HUD, fixed icon slot, display-only drink sequence and focused review/tests | Verified every supplied 256×256 RGBA frame and copied the set unchanged. The bottle replaces the fruit in the same icon render pass below CRT, with zero added rotation and existing reveal/layout behavior. Counts select exact frames; rollover briefly shows full, drains the image to zero and then reveals any newly earned milk. The adjacent number never participates in the drink countdown. Browser review covered empty, half-full, full at actual count zero, life award and empty after draining without console errors. Focused frame/loading/rollover/bonus/reset/independent-texture and HUD-pipeline checks passed, as did the lightweight build. No full suite was run. |

| 110 | Replace plain wooden crates with folding milk cartons | ~18 min to verified weight-driven contact; full-render and release checks followed | Automatic from `main` | User clarified that the downstroke must follow Boneman’s landing speed | Shared carton geometry/print/cardboard material, column exposure, sole-driven folding, visual foot seating and focused review/tests | Cream bevelled cube bodies and red MILK print replace plain wood, including the counter icon. Only the highest solid crate in a column has an open gable; hidden lids unfold after the cover breaks and correctly restore across ghost switches and resets. Descending soles constrain cap height directly, share render interpolation, and complete the fold with the existing stomp snap before the pop effect. Visual shoe seating closes the airborne pose gap without changing the movement collider, bounce, rewards or stack settling. Focused coverage includes 336 weight-contact samples at 30/60/120 Hz, three actual landings, held weight, adjacent misses, mixed stacks, net-zero-count state changes and run-mode restoration. Existing crate, character interaction, HUD and fixed-render checks pass. Lite/full browser review verified shape, stack exposure and visible foot contact. No full suite was run. See `docs/MILK_CARTONS.md`. |
| 111 | Refine the map UI skateboard and expose level-name size | ~12 min to verified browser preview (estimate); release checks followed | Automatic from `main` | Supplied partial tuning, with an explicit symmetric modern profile requested | Independent map preset, plain grip texture, live title sizing and focused checks | Applied the supplied map dimensions, scale, finish and hardware sizing, matching both elliptical tips, body transitions, kicks and truck spacing for a modern popsicle profile. The centre stripe is omitted from a separate cached map grip texture; gameplay defaults remain unchanged. Skateboard Lab → Map UI Board → Map level name exposes a 48–190 px title control, with long-name fitting, JSON import/export, reset, browser persistence and no geometry rebuild for text-only edits. Existing saved profiles are preserved; Reset this board loads the revised preset. Focused board/profile/card checks and the lightweight build passed. Real Chrome review verified the slider, reload/reset, gameplay-tab isolation, kickflip, lite/full desktop, touch Play and CRT with no console errors. No full suite was run. |
| 112 | Match competition ground-camera framing to the main game | ~25 min to browser comparison (estimate); focused checks and release followed | Automatic from `main` | User explicitly requested preservation of the calibrated vert swing | Optional ground framing, slope handoff, focused comparison tests and local browser review | Normal park travel reads the main camera height, distance, pitch and lens while retaining behind-rider tracking. The final shot blends back to the original rig by 35° of ramp slope and throughout vert air; internal vert orientation, pivot smoothing and landing timing stay intact. Exact comparison covers 163 steep/vert samples; real runtime checks cover four perimeter launches/returns, 16 rail catches and 553 grind frames, plus frame-rate parity. Browser comparison confirms identical frozen-apex pose/lens and the higher ground shot, with no console errors. Lightweight build and focused camera checks pass. No full suite was run. |
| 113 | Add Quaternius swimming and a Jungle Ruins starting cove | ~25 min to playable water/animation review (estimate); immersion refinement and release checks followed | Automatic from `main` | User requested a lower moving stroke after visual review | CC0 swim loops/import aliases, water movement/buoyancy, animation routing, shoreline/perimeter data, camera, wake effects and focused checks | Imported the free Standard pack’s forward-swim and tread-water loops at native 30 FPS and added them safely to saved animation catalogs. Walking into deeper water stows the board and starts swimming; neutral treads, held Jump increases speed/cadence, and the rising beach restores walking. The rear jungle cap opens into a sandy cove with palms, rock headlands, a continuous seabed and enclosed playable water. Moving strokes sit 35 cm deeper after feedback; idle height is unchanged. The camera follows the swimmer and hides the landing marker. Focused checks cover real entry/return, knockdown water entry, five fast boundary approaches, frame-rate parity and capture. Lite/full browser review verifies motion, waterline and wake; level checks and the lightweight build pass. No full suite was run. See `docs/SWIMMING.md`. |
| 114 | Deepen swimming, soften water ripples and fix looping fatal falls | ~30 min to visual review (estimate); contact refinement and release checks followed | Automatic from `main` | Moving stroke lowered another 20 cm; user requested wormhole-derived ripples and a simple fatal fall | Shared contour/band math, batched foam and splash pools, one-shot Death01 import, fatal-pose routing and exact contact seating | Moving root immersion is now 1.53 m; idle remains 1.65 m. Ripples reuse the wormhole’s independently deforming contours and five-row feathered bands in one transparent mesh; entry droplets use a second instanced draw. Fatal death uses Quaternius’s free CC0 Death01 once and holds its real final frame. Removed the endless 13-rad/s rotation and fatal ragdoll/recovery overlap, while retaining recoverable skate bails. Exact animated-vertex clearance and a small head/heel support tilt avoid floor clipping and bound-corner hover. Focused checks cover 960 fatal-pose frames, pool reset/softness, swimming return/boundaries and unchanged life/Game Over/bonus flow; the lightweight build passes. Lite/full browser review completed without runtime errors. No full suite was run. |
| 115 | Prevent milk turning black late in descending levels | ~12 min through before/after browser reproduction (estimate); release checks followed | Automatic from `main` | 0 | Milk material depth-fade opt-out, scenery decorator guard, regression and local review | Reproduced solid-black milk on Test Course’s row around Z=-1101/Y=-20.7. Jungle had attached its world-height black-pit effect to the globally shared milk material; every later orb below Y=-10 inherited it. Milk now explicitly opts out of that scenery-only modifier. The original creamy shader, native fog and dark pit scenery remain. Focused checks cover all 69 low Test Course pickups, crate drops, clones and level disposal. Lite/full browser review confirms white shaded drops at the failing row and near Z=-2139, with no runtime errors; the lightweight build passes. No full suite was run. |
| 116 | Add Level Select and establish TV-safe game menus | ~30 min to first controller/UI review; ~2 hr through subsequent design revisions (estimates) | Automatic from `main` | User supplied Crash-style list/preview reference and required fixed TV segments, comic prompt-kit hints, unboxed heading/controls and visual collectible ownership | Shared GameFlow/competition layouts, renderer-based rewards, captured level previews, campaign entry, Touchpad input, focused checks and browser review | Pause and map now expose Level Select; PS4/5 Touchpad, View/Share or Tab opens the map list. Full-screen composition places one of 12 actual level previews over the level list, with earned rotating game meshes or recessed sockets and best times at right. Header/footer are unboxed; no redundant Level Select title. Up/down chooses playable rows; left/right pages unlocked islands and remembers selection. Play rechecks unlocks and preserves inventory/bonus rules through the existing loading guard. Pause fills the frame; Progress pages islands and uses the same collectible models. Competition guide has four controller-accessible pages, with fixed header/actions and bounded content. All 14 GameFlow screens were checked at five viewport sizes (720p, 1080p, 4:3, phone and short landscape): no root overflow or off-screen controls; only the shortest results tally needs internal scrolling. Fixed direct-render DPR scaling, mirrored clipping and preserved CRT layering. Controller entry, held-confirm protection, menu/input/competition focused checks and lightweight build pass. No full suite was run. Design policy recorded in `docs/MENU_DESIGN.md`. |
| 117 | Replace the five-bounce crate with a blue milk rack and nine bottles | ~20 min to assembled in-game review (estimate); bounce and release checks followed | Automatic from `main` | User supplied both Meshy GLBs and requested identical cube proportions with two bottles disappearing per bounce | Texture-sized GLB derivatives, shared/instanced prop presentation, existing multi-hit/checkpoint/run-mode integration and focused review | Blue crate plus upright 3×3 bottles fits the original 0.96 m cube. After user review, the rack rises to 82% of cube height and bottles widen to 27% of its width to reduce exposed necks and gaps. Visible counts are 9/7/5/3/1, followed by the regular final crate smash. Player movement, collider, rewards and force-smash paths retain existing rules. Checkpoints restore partial contents and hard reset refills them. Ghost/run-mode appearances remain reversible. Supplied geometry stays intact while texture payload falls from 8.3 MB to 436 KB; a full crate uses two draws and 5,172 triangles. Focused real-player crate tests and build pass; lite/full browser review checks shape, dimensions, bottle depletion, five live bounces and final smash. No full suite run. See `docs/MILK_CRATE.md`. |
| 118 | Replace TNT and Nitro with imported dynamite bundles | ~25 min to lit-fuse/material review (estimate); colour-mask refinement and release checks followed | Automatic from `main` | User supplied red/green Meshy exports and references, requesting a burning TNT rope fuse and clean Nitro warning stripes | Shared bundle geometry, independent colour-pulse shaders, braided burn-front fuse/ember/sparks, geometry-owned warning bands and existing crate lifecycle | Both imported bodies retain the 0.96 m contact cube. TNT burns a braided rope from its free end down over the existing three-second countdown and pulses only red pigment on its audio beats; tan caps/straps remain steady. Nitro has no fuse, pulses green continuously and receives clean antialiased perimeter warning stripes on its actual straps, replacing the malformed colour/normal detail. Existing explosions, trigger rules, outlines, resets and run modes remain. User review removed Nitro bob and raised the minimum pulse to an always-glowing baseline. Shared textures shrink the two exports from 11.9 MB to 932 KB. Focused Level/timer checks, build and lite/full browser review verify independent countdowns, burn endpoints, stripe continuity and shader health. No full suite run. See `docs/EXPLOSIVE_BUNDLES.md`. |
| 119 | Run the full suite after the menu and collectible replacements | ~26 min to complete all checks; release verification followed | Automatic from `main` | User explicitly requested the full suite | Full check chain, targeted fixture corrections and resumed remaining checks | All 42 check groups passed with no skipped groups. Updated four stale test files: the gameplay mock DOM lacked selector methods; the animation fixture expected 20 clips/zero-index idle instead of the 23-clip catalog and swim-to-idle blend; the fixed-render audit expected the old single menu-composition path; and the mobile audit expected retired whole-panel scrolling/close-X controls. Rechecked each correction and resumed from the failure without repeating completed physics stress runs. Coverage includes level traversal, campaign/save flow, vert/grind/bail/tricks/overtime, milk and explosive crates, editor security/round trips, character/animation, ocean/CRT/HUD/input, skateboard and effects. Latest Nitro correction is stationary with an always-glowing eased pulse. |
| 120 | Give Roo HUD text real chamfers, responsive light and a matching PNG image font | ~24 min to matching lite/full game review; export and release verification followed | Automatic from `main` | Supplied Crash HUD reference; preserve Roo and omit the heavy black surround | Vector extraction, robust polygon bevel, lit material lab, supersampled atlases, shared DOM/Canvas cap metrics and focused browser review | Built real inset faces and bevels from RooRegular.ttf, resolving narrow-tip intersections instead of stacking outlines. Green/cobalt and yellow/orange/red materials export the same treatment to transparent 52-glyph image fonts. The live lab supports gentle tilt, light flicker, pointer lighting, reduced motion and PNG/ZIP export. Gameplay uses the atlas in its existing pre-CRT canvas, and lite mode uses identical cropped glyphs. Unified cap height, bearings and baseline; documented measured reference sizes and separate word/specimen tracking. All baked glyphs preserve the font silhouette (worst pixel-mask overlap 97.9%); dark/light, mobile and lite/full Chrome reviews were error-free. Focused HUD checks and the lightweight build pass; no full suite was run. See `docs/ROO_HUD_TREATMENT.md`. |
| 121 | Remove the fatal-animation CPU spike | ~45 min to profiled and reviewed implementation (estimate); release checks followed | Automatic from `main` | User reported death lag and requested canned grounded deaths, reserving physics/ragdoll for unsupported falls | Death-only update path, cached sparse support probes, settled-pose hold and airborne ragdoll handoff | Measured the previous fatal step at ~11.5 ms, with ~7.2 ms spent repeatedly fitting animated vertices to the floor; gravity/wall work was ~0.02 ms in the isolated fixture. Supported deaths now stop movement, retain cached support and skip all full-character bounds/seating scans. After the canned fall, the final pose and offset are held. Unsupported deaths keep necessary fall contacts, then blend into the established ragdoll if still airborne after 1.2 s. Lost floors release the corpse; movers/crate lids retain support ownership. Life/Game Over/bonus/flying-reward timing remains. Revised 126-step CPU profile is ~1.4 ms average; live held-pose player updates ~0.08 ms. Focused cost/ownership, large-head clearance, death lifecycle and 960-pose checks pass; no full suite requested or run for this change. See `docs/SWIMMING.md`. |
| 122 | Rebuild Roo lettering toward the supplied reference with a model pass for every glyph | ~4 h 32 min through complete local font/HUD review, including model and edge iterations | Automatic from `main` | User rejected the v1 approximation and required per-glyph image-model processing for a PNG font | 51 model sources/prompts, reference-preserving bake, two RGBA atlases, optical metrics, shared DOM/Canvas typography, provenance ZIP and focused review | Every nonempty Roo glyph has a separate model pass; original contours supply antialiased alpha. The bake retains the reference lighting/color where available, uses model detail on the raised faces, and removes black-matte edge contamination. Added two 384-pixel-cap colorways with hashes/prompts, exact BONUS optical placement, reference-scaled counters and a full-size aligned denominator. Portrait bonus counters occupy separate rows. Native/enlarged dark/light and real lite/full game reviews completed; all glyphs/exports, identical palette alpha, zero magenta contamination, reference viewport sizes and changing-count stability pass. Focused HUD/visibility/combo checks and build pass; no full suite run. See `docs/ROO_HUD_TREATMENT.md`. |
| 123 | Review Roo glyph errors, add three highlight bakes, tighten adjustable spacing and use the PNG font in menus | ~2 h 30 min through local font/menu verification (estimate; prompt timer not captured) | Automatic from `main` | User accepted the direction and requested polished close-up gradients, light-frame crossfades, tighter tunable spacing and menu use | 21 fresh per-letter cleanup passes, corrected palette/matte/crop handling, analytic vector alpha, three light states, shared saved appearance controls and menu integration | Reviewed all 51 glyphs in two palettes. Fixed row-derived color bands, P registration, matte fringes and mesh coverage errors on punctuation. All three light frames share exact alpha; Canvas and actual-browser crossfades retain edges within one 8-bit alpha step, including overlapping text. Counters keep reference size with a shared adjustable denominator gap. Font Studio settings persist and propagate across tabs; reduced motion is honored. Checked 50 menu layouts plus world-map and Jungle Cup screens, exports, lite/full HUD and source provenance. Focused checks/build pass; no full suite run. Built-in image-model identity/quality is unverified and recorded as such. |
| 124 | Distinguish Roo zero from letter O with the supplied inner accent | ~24 min prompt-to-playable through local font/HUD validation; release verification followed | Automatic from `main` | User supplied a short rising stroke inside the zero | Authored accent vector, matched face color and bevel lighting, six v4 atlas frames, provenance and focused pixel check | The stroke joins the inner left wall and tapers before the right, with matching antialiasing across both palettes and all three light frames. Every pixel outside zero is identical to v3; existing opaque zero pixels, size, bearings, advance, kerning and saved spacing remain unchanged. Reviewed 0/O at 32–330 px, PNG/ZIP exports and lite/full HUD; alpha parity, clean masks and build pass with no browser errors. No new model pass or full suite run. |
| 125 | Rebuild the zero from its flat Roo letterform with a broader integrated accent | ~10 min prompt-to-playable through local font/HUD validation; release verification followed | Automatic from `main` | User rejected the thin v4 overlay and required the complete zero pipeline again | Integrated flat counter contour, fresh whole-glyph image-model pass, complete bevel/material/light bakes, six v5 atlases and provenance | Accent area grows from 1,022 to 3,029 mask pixels with a broad connected root and open right gap. Removed the separate accent renderer; the entire zero is reshaded as one glyph. All other glyph pixels, zero outer contour, size, bearings, advance and saved spacing stay unchanged. Both palettes and three frames reviewed at 32–330 px; fresh-source, mask, alpha, exports, lite/full HUD and build checks pass without browser errors. No full suite run. |
| 126 | Preserve approved Roo artwork and replace procedural lighting with direct model glisten edits | ~3 h 46 min through complete local artwork/font/HUD review, including image generation and the lighting correction; release verification followed | Automatic from `main` | User approved all neutral letters, rejected the derived lighting and requested model edits of those exact neutrals | 51 complete neutral masters, 102 direct glisten edits, full-artwork RGBA assembly, fixed alpha, source provenance and live inspection controls | Retained the approved accented zero and rebuilt the other 50 neutrals without clipping finished bevels to flat font vectors. Each glisten uses only its approved neutral; all inferred-normal relighting was removed. Audited every glyph/angle in both palettes, including detached punctuation and light backdrops. All six 512 px-cap atlases share identical alpha; Canvas/SVG fades stay within one alpha level. Actual preview pixels animate, the slider follows them, and pause/manual/reduced-motion/zero-strength states are explicit. Saved tight spacing, reference counter sizes and PNG font use across HUD/menus remain. Source/export, 50 menu layouts, cross-tab controls, lite/full HUD and build checks pass. No full suite run. See `docs/ROO_HUD_TREATMENT.md`. |
| 127 | Calm the green/cobalt font and make the main title large and orange | ~18 min through local font/title review; release verification followed | Automatic from `main` | User praised orange, called green/cobalt overbright/oversaturated and requested a large orange main title | Perceptual palette grade, byte-preserved orange atlases, v7 package, responsive launch-title sizing and orange studio default | Green/cobalt now follows the approved gold lightness with softer face contrast, lower chroma, smooth hue transitions and protected bright glints. All three orange PNGs are byte-identical to v6; every palette/frame retains the same alpha, shapes and spacing. BONEMAN uses the orange palette and a larger responsive title region, with menu controls in a separate compact column. Font Studio opens on that orange title. Reviewed dark/light color proofs, fixed-alpha/exports, 50 menu layouts, 10 launch layouts with three actions, actual glisten controls, lite/full HUD and build. No full suite run. |
| 128 | Make Roo glints visible, restore reference colours, add generated curved balance meters and a jump-only Meshy bonus platform | ~1 h 54 min through local artwork/gameplay and campaign-placement review; release verification followed | Automatic from `main` | User requested stronger glimmer, original green/cobalt colours, a stone circle requiring a deliberate jump, and image-generated punk stop-motion gauges with a glove marker | V8 font grade/glints, migrated 4.2 s full-range light cycle, four generated gauge cels and glove, shared DOM/Canvas meter, Meshy stone prop and gated circular landing | Approved orange neutral and every glyph alpha remain unchanged. Green/cobalt returns to the sampled reference range; only newly lit model glints gain strength. Meters preserve grind/manual/lip controls and pointer polarity while using per-frame measured paths, irregular held cels and reduced-motion behaviour. Meshy produced a 2,511-triangle stone platform, packed to 512 KB; its 1.05 m raised top and circular sides block run-over entry. A rising jump command arms one grounded top landing, with reset/trial/lock guards. Default placements stay beside the route, check clearance and retain supported returns. Real keyboard walking/jumping and lite/full HUD checks, meter pixel/pose checks, complete font/source/alpha/exports, bonus-flow tests and build pass. No full suite run. |
| 129 | Consolidate map Options and pause collectibles, simplify input hints, and sharpen menu type | ~25 min through local menu/playable review; release verification followed | Automatic from `main` | Level Stats is a rename and must still enter levels; confirmation applies to abandoning an active run | GameFlow UI/surface/layout, map actions, M-dismissible text panel, level-entry inventory, focused menu tests | Removed gameplay text-tuning entries and the separate Progress action. Options and Pause put controls left and the overall collectible sheet right, with sockets/earned models and no progress bar. Select and clickable Back are the only submenu hints; no Back list rows or Choose hints; touch uses a corner close action and hides hints/gameplay pads. Map Level Stats still selects and enters levels. Pause Level Select defaults to Cancel before forfeiting unfinished boxes, score, milk/lives and run rewards; saved campaign rewards survive, with forfeiture applied only after the destination builds. Menu text reuses the HUD PNG atlas at full target resolution, handles native DPR, and ignores nested PNG decoration to eliminate duplicate painting. Forty responsive lite/full layouts, touch entry/back, M dismissal, actual confirmation/cancel/forfeiture, native 2560×1440 menu output, focused flow/prompt checks and build pass; no full suite run. |
| 130 | Restore the earlier green/teal font hues without contrast or saturation alterations | ~13 min through local colour and runtime review; release verification followed | Automatic from `main` | User identified the v7 screenshot as the better hue treatment and asked for original neutral artwork tone | V9 green hue-only grade/export, source-tone invariants, font metrics, studio preview links and font review scripts | Restored the v7 green/teal/cobalt hue curve and removed its face-lightness compression/chroma scaling, the v8 pigment remap and extra green glint amplification. Original image-model lightness/chroma and unboosted lighting edits supply all three green frames, with chroma reduced only to fit sRGB without channel clipping. Every approved bevel/alpha/spacing value is retained; all three orange files are byte-identical to v8. Reviewed all 51 glyphs and light frames, source tonal-error/chroma bounds, alpha/component/matte checks, exports, actual animated/pause/manual controls, lite/full HUD and source-sized BONUS. Build passes; no full suite run. |
| 131 | Replace the rejected focus pass with measured PNG colour filters and live sliders | ~50 min through reference-frame measurement and local checks; release verification followed | Automatic from `main` | Restore the prior beautiful PNG rendering; normal orange remains the untouched neutral image; inactive text is desaturated; provide sliders | Reference analysis/profile, draw-time PNG colour filters, menu focus frame scheduling, M-only sliders, browser verification | Reverted the previous cached-ink implementation. Downloaded the supplied public reference for local analysis and measured its native 30 fps frames at 36–38 s: one white frame, three orange frames. Original PNG painter/decoder/metrics/assets are unchanged. Normal selected text has no colour filter; other states use standard CSS/Canvas filters over the existing neutral orange PNG. No pixel rewriting, replacement lettering or new font bake. Six saved M-dismissible sliders control rate, white duty/brightness/desaturation, and inactive saturation/brightness; reset restores reference timing and untouched-orange defaults. Removed extra selection indicators. Sixty actual displayed frames match the measured cadence in lite/full; controller input, white exposure, zero alpha/source change, slider persistence/reset, reduced motion, idle behaviour, focused flow/input checks and build pass. No full suite run. |

| 132 | Drop into vert after a downhill coping balance failure | ~30 min prompt-to-playable through local runtime and visual review; release verification followed | Automatic from `main` | Rider should descend the pipe like a lip release, without an angled ollie or bail | Face-validated coping handoff, normal-directed surface riding, brief stale balance-input grace, deck-side bail correction and local review controls | Samples the real transition just below coping, corrects analytic pipe normals and hands directly to existing grounded skating without upward impulse. Keeps board, grind combo and supported follow-camera routing. Covers curved Jungle Cup walls/corners and both analytic pipe axes in park/campaign controls; flat deck tags cannot suppress an outward bail. All 51 focused cases and 1,260 descent frames pass, plus existing grind-camera and rail-air regressions, build, and lite/full browser review with no console errors. No full suite run. |

| 133 | Clean up Jungle Cup presentation and make grind trick inputs reliable | ~20 min prompt-to-playable through local UI, geometry and skating review; release verification followed | Automatic from `main` | Remove inline welcome controls and live bail count; use one large Roo PNG countdown; fix coplanar texture fighting and missed grind tricks | Scoped countdown selectors/responsive PNG sizing, leaner competition HUD, authored render-depth underlay, immediate grind switches, buffered rail-ollie flips and completion-aware catches | Countdown digits retain one atlas label at 720p/1080p, 4:3 and compact portrait/landscape, below CRT. Existing points/avatar/special meter remain. Foundation layering resolves 1,108 sampled overlaps across eight surface pairs while keeping all support heights unchanged and round-tripping through level data. Seven rapid grind switches, 24 directional Square chords with up to 150 ms lead, existing vert-trick/layout/UI checks and 51 coping drop-in cases pass, plus check:levels and build. Full-render live chains register 50-50 → Boardslide → Nosegrind → Feeble and 50-50 → Heelflip → 50-50; no browser errors. No full suite run. |

| 134 | Regenerate the balance pointer with three fingers and pivot it along the gauge | ~20 min prompt-to-playable through generated-art, export and gameplay review; release verification followed | Automatic from `main` | Match the cartoon glove anatomy and rotate off-centre around the pointing fingertip | Built-in image-model glove edit, clean matte/alpha export, measured new tip, shared curve-normal pointer transform and focused pose review | Three fingers plus thumb (extended index, two curled fingers, folded thumb), with the approved ivory/ink treatment. Packed as a 320×182 RGBA sprite with intact dark contours and no magenta pixels. Both meter orientations derive a restrained inward tilt from each existing cel’s broad curve; the renderer translates to the contact before rotating, keeping the indicated balance exact. All 1,608 focused pose samples pass, alongside the build and dark/light, animated and lite/full gameplay browser checks with no errors. Existing meter cels, balance physics and input polarity are unchanged. No full suite run. |
| 135 | Render button-hint icons at Retina resolution and optically align them | ~45 min prompt-to-playable through corrected full-render review | Automatic from `main` | User requested no full suite, identified the homepage 1× result and calibrated the vertical alignment | Physical-pixel direct interface surface, scale-independent optical hint offset and focused checks | The PNG sources were already 128×128—more than enough for the 56 physical-pixel homepage ENTER—but the separate hint-only canvas was incorrectly allocated in CSS pixels while the menu and renderer used a 2× drawing buffer. The direct interface path now separates its physical Retina raster from Three.js's logical null-target viewport, so hints match the rest of the homepage instead of being painted at 1× and enlarged. Render-target/pre-CRT paths remain at their exact target dimensions. The reviewed 28 px hint sat low at a measured -1.2 px and high at -11.2 px; the final -22% transform is their -6.2 px midpoint expressed relative to the rendered icon box, so it scales with every hint size and viewport. Other gameplay prompts are unchanged. Corrected full-render title, map and Options review showed a 2× interface raster and clean console. Prompt, fixed-render and GameFlow-surface checks, responsive menu layouts, TypeScript and the lightweight production build pass. No full suite was run. |

| 136 | Make balance progressively accelerate toward immediate failure | ~25 min prompt-to-playable through tuning review, runtime checks and lite/full browser comparison; release verification followed | Automatic from `main` | Linear-feeling balance and rescue buffers made sustained grinds too easy | V19 cubic edge pull, integrated needle momentum, harder fast-grind defaults, zero default entry/end grace, retired stale-direction and false rail-drop saves | Edge pull grows as position cubed, with gravity 6 and inertia 0.85. Fast grinds retain 70% of base instability; capped time difficulty leaves centred ordinary-speed balancing possible. Uncontrolled test grinds fail in 0.78–0.83 s and held-wrong grinds in 0.28 s; an active twelve-second sequence stays within 0.158 without a powerup. Early correction at 45% recovers, while a late 90% correction bails; grind/manual/lip crossings resolve on their crossing step. Verified coping drop-ins and earned powerups remain. Factory-default, dynamics, coping-drop and trick-input checks plus build pass. Browser confirms active defaults, recovery/bail outcomes and no errors. No full suite run. See `docs/BALANCE_TUNING.md`. |

| 137 | Remove residual chroma reduction from teal/green Roo text | ~20 min through colour and font review; release verification followed | Automatic from `main` | User wanted no desaturation and no global saturation boost | Uncompressed hue conversion, v10 green atlases and package, unchanged orange frames, current-version review checks | The v9 gamut-fitting branch still reduced chroma despite its 1× saturation setting. V10 bypasses that reduction while keeping source lightness/chroma inputs, the earlier hue curve and 1× gain; standard RGB encoding bounds channels only. No exposure, contrast or highlight boost is added. All three orange PNGs are byte-identical to v9; glyph shapes, spacing and alpha are unchanged. Full 51-glyph/three-light audit, component/matte/alpha/export checks, actual green lighting controls and lite/full HUD checks pass. Mean green brightness changes by less than one 8-bit level rather than receiving a global boost. Build and public verification follow; no full suite run. |

| 138 | Complete timed skate runs through landing, dismount and the score tally | ~45 min prompt-to-playable through lifecycle, audio, animation and lite/full browser review; release verification followed | Automatic from `main` | Beep through the final three seconds and finish naturally after the last action rather than cutting to judging | Exact 3–2–1 SFX cues, safe-stop gate, explicit finishing phase, authored dismount, actual HUD cash-in completion and reset cleanup | Unscored airs, grinds, manuals, flips, grabs, combos and bail recovery can finish at 0:00. A supported 0.6 s hop leaves the board parked beside the skater, with gameplay input/remount disabled; a post-dismount beat and the real displayed score transfer gate judging. Final cash-ins do not expire early. Browser traces show landing before dismount and the full 37,234 score settling before judges, with one 3–2–1 cue sequence and no console errors. Focused event, overtime, finish, audio, bare-spin and UI checks plus build pass. No full suite run. See `docs/SKATE_RUN_FINISH.md`. |

| 139 | Ease balance correction and retain balance through linked trick re-entries | ~22 min prompt-to-playable through focused runtime and lite/full browser review; release verification followed | Automatic from `main` | Keep the accelerating balance structure, make correction slightly easier, and grant only 10% relief on a linked catch | V20 modest correction gains, combo-owned needle/momentum/fatigue history, shared grind/manual/lip re-entry, tuning control and live review scenarios | Grind/manual/lip correction rises 7.1%/5.3%/4.8%, with cubic pull, inertia and zero bail grace unchanged. Each actual re-entry retains 90% of offset and velocity, all balancing age and noise phase; air pauses history, style changes grant no relief, and bank/break/respawn starts fresh. Six real ollie/flip re-catches, both signs, cross-mode links/reset boundaries, twelve-second active balancing, 24 Square chords and 51 coping drop-in cases pass. Full-render browser shows 0.558 → 0.502 with 90% momentum and unchanged age; early correction recovers and late correction bails. Factory defaults, build and console checks pass; no full suite run. See `docs/BALANCE_TUNING.md`. |

| 140 | Share the competition Trick Guide across pause, map and home Options | ~18 min prompt-to-playable through menu routing and lite/full browser review; release verification followed | Automatic from `main` | Expose the existing guide from every Options entry and match home Options to map content | Shared four-page trick catalogue, home Options entry, nested guide/back routing, controller paging, dynamic special-command prompts and pre-CRT guide text | Home/map share play mode, sound, music, prompt style, collectibles and the guide; gameplay keeps its existing play-mode restriction. Back restores the guide row in Options and then the originating menu, without starting a competition or resuming gameplay. Visual-order controller navigation, held-edge paging, actual paused skating, competition entry, 20 guide layouts and five Options layouts pass. All new guide pages fit without scrolling at 720p/1080p, 4:3 and compact portrait/landscape; competition pages also fit at 720p. Focused GameFlow interaction/surface and competition UI checks, build and clean browser console pass. No full suite run. |

| 141 | Polish competition restart, judging, heat atmosphere and counters | ~24 min prompt-to-playable through scoring regressions and lite/full browser review; release verification followed | Automatic from `main` | Clarify whole-event restart, forgive a couple of bails on huge scores, penalize long inactive periods, vary each heat and remove irrelevant counters | Competition-aware pause label, capped first-two-bail relief, regulation-only inactivity accumulation, three runtime lighting/sky looks and avatar-only HUD painting | At 48,000 points the first two bail deductions shrink 80%; later bails keep their full marginal cost. Twelve seconds of setup grace precede a squared gap deduction: 20 seconds costs eight marks, retained across later tricks. Pause/countdown/overtime/tally are excluded. Day, warm low-angle sunset and readable blue night retain authored geometry and reset to day on a whole-event restart. Life/death numerals and labels disappear in DOM and Canvas while portrait/SPECIAL remain; ordinary counters restore outside competition. Focused judging, avatar painter, event/overtime, UI, finish and visibility checks plus build pass. Full-render heat comparisons, real heat-three restart and live scorecards pass with no console errors. The broader focused HUD pipeline check reaches a pre-existing stale Text Tuning selector assertion, verified unchanged on main; left outside this task. No full suite run. |

| 142 | Investigate the harsh replay catches and audit grind/manual tuners | ~38 min prompt-to-playable through replay analysis, slider audit and lite/full browser review; release verification followed | Automatic from `main` | Fresh and linked grinds fail too quickly; sliders appear ineffective or stale | Per-catch easing distinct from retained combo age, full inward correction, gentler cubic pressure/momentum defaults, live settle controls, exposed manual landing buffer, range/readout fixes and endpoint catch guard | The replay records the user's four tuner edits at frame 7,510. First-heat input windows failed uncorrected in 0.350/0.317/0.167 s; isolated v21 runs last 0.983/0.850/0.833 s while preserving exact 90% linked state. Removed cached speed-scaled calming and old inward-input suppression, kept zero peg grace, and rejected two-tick outward endpoint catches. Shared controls are grouped and every grind/manual control is documented by effect and application phase. Real slider edits change needle response with eight seconds of combo age; save/reset bounds, exact slider step alignment and replay begin/change/end refresh are checked. Focused dynamics, re-entry, motion/slider, factory, trick-chord and 51 coping-drop cases plus build pass, with lite/full browser review and no console errors. No full suite run. See `docs/BALANCE_TUNING.md`. |

| 143 | Stop head bobbing and flipping during curved grinds | ~28 min prompt-to-playable through replay reproduction, pose isolation and lite/full review; release verification followed | Automatic from `main` | Character head oscillates wildly while grinding | Independent procedural head smoothing, shortest-angle yaw response, reset cleanup, current-tangent rider facing and focused replay/pose regression | Quaternion restoration could re-express a sideways head as flipped XYZ Euler angles; reading those angles back into pitch smoothing produced real 160–173° alternating snaps. Pitch/yaw now remain independent of rendered/authored bone rotations, with balance counter-roll and authored head tracks preserved. The rider also follows curved rail tangents instead of its stale catch heading. Twenty-four pose/restoration cases peak at 0.751° per settled step, with under 0.001° rendering divergence; the supplied 3,603-step replay retains 262 grind frames without flips. Existing trick and grind-camera checks, build, and mirrored lite/full browser comparisons pass with no errors. No full suite run. See `docs/GRIND_HEAD_ANIMATION.md`. |

| 144 | Add a four-slot save grid with last-finished-level previews | ~42 min prompt-to-playable through save migration, menu layout and lite/full browser checks; release verification followed | Automatic from `main` | Save menu should be a 2×2 grid of four slots, each showing the last finished level | Backward-compatible fourth slot and completion metadata, stable legacy fallback, shared New/Load/Save grid, cached preview imagery beneath CRT and 2D navigation | Tracks repeated normal finishes, valid non-record trials and complete competitions separately from map focus, with previews tied to durable save snapshots. Existing three-slot progress survives fourth-slot creation; failed/manual saves and discard semantics are covered. Four-way navigation follows the grid, disabled load slots are skipped, cancellation restores its originating grid/slot, and Save Slot N explicitly names the active destination. All 15 New/Load/Save layouts fit at 720p, 1080p, 4:3 and compact portrait/landscape without scrolling or off-screen controls. In-memory browser fixtures verify picture refresh after Save and confirmation routes without touching user saves. Focused campaign/preview/GameFlow checks, build and full-render review pass with no console errors in the final pass. No full suite run. |

| 145 | Separate skate-park speed, ollie and camera tuning | ~19 min prompt-to-playable through movement comparison, independent controls and lite/full review; release verification followed | Automatic from `main` | Jungle Cup feels slow, charged speed barely differs and ollies read small; preserve platforming feel | Twelve park-only sliders selected by the skatepark flag, corrected speed-lens endpoints, independent height/time ballistics, ordinary-air camera follow and replay default isolation | Confirmed existing park targets of 11.303/15.329 m/s versus platforming 12/23, near-identical full ollie heights (~1.76/~1.78 m), different arcs and vertical camera follow. Fixed the six-degree zoom previously paying only ~1.32° at park charged speed. New physics multipliers default to 1; platform settings and calibrated vert behavior remain. Live browser edits reach 22.993 m/s at 1.5× charge and 2.63 m at 1.5× height. Focused checks cover actual platform trajectory isolation, future non-competition skateparks, long charge timing, replay capture/restore, slider metadata, 163 protected camera samples, four real vert routes, factory defaults and speed-camera behavior. Build and lite/full 16:9 review pass; no full suite run. See `docs/SKATE_PARK_TUNING.md`. |

| 146 | Use absolute park tuning and the platforming skating rules | ~20 min prompt-to-playable through shared motor/air integration and lite/full review; final validation and release followed | Automatic from `main` | Replace park multipliers with absolute values matching platform defaults; share speed/control/decay behavior and remain mounted at a stop | Live per-player tuning view, common drive/brake/steering/friction/slope and ordinary-ollie paths, absolute physics/camera schema, retired multiplier filtering and updated regression fixtures | Defaults now use platform values directly: cruise/charge 12/23 m/s, tap/full pop 6.5/11 m/s, 0.4 s charge, board gravity 33/70 and ground-anchored ordinary camera follow. Fourteen full movement traces match across headings, four ollie charge lengths match throughout their arcs, and idle/both brakes hold zero without dismounting. Park-only edits remain isolated from platforming, including recorded tuning playback. Kept player-relative park framing, true vert wall tracking, angled returns, calibrated vert pop/gravity and camera swing; shared slope pumping determines entry energy. Verified live 26 m/s entry, 1.779 m full ollie and stationary mounted pose in lite/full render. Focused parity, factory, camera and coping-drop checks and production build pass. No full suite run. See `docs/SKATE_PARK_TUNING.md`. |

| 147 | Roll back the skating overhaul and narrow park parity to drive settings | ~14 min prompt-to-playable through restoration, focused checks and lite/full review; release verification followed | Automatic from `main` | Restore the previous park feel/camera, reduce sliders, match speed/acceleration and coast to a mounted stop | Original park motor and camera restored; only four absolute drive controls, five camera controls and a no-input rollout branch remain | Rolls back iteration 146's shared motor, steering, slope pumping, asymmetric ollies and ground-anchored air camera. The camera implementation is byte-identical to `3d78c24`; player differences from that baseline are confined to the tuning import and the small motor block. Targets are 12/23 m/s with 10/9 m/s² pickup, and zero input removes drive so platform-style flat rollout reaches zero without dismounting. Steep surfaces retain park gravity/drag; the original 0.2 s ballistic ollie, turn/brake response, tracking, tricks and recovery remain. Nine-control schema, speed/rates, full flat coast, stop/restart, restored responses, profile isolation, retired-key filtering, factory, four camera/vert routes and 51 coping drop-ins pass. Build and lite/full browser review pass; no full suite run. See `docs/SKATE_PARK_TUNING.md`. |

| 148 | Fix crossed crouch legs and verify the rejected first attempt | ~13 min prompt-to-playable through measured stance correction, saved-draft migration and lite/full browser review; release verification followed | Automatic from `main` | User reported crossed legs and requested an independent check of the prior fix | Pelvis-space hip direction correction, catalog v22, tolerant untouched-clip migration and reproducible source/guess/fixed review | Original knees cross by 0.160 rig units; the previous local-Z hip/knee offsets leave only 0.004 between centres. The accepted correction turns each entire leg outward from its measured thigh direction, preserving knee/ankle keys, source elevation, upper-body motion and timing. Default knee/ankle gaps remain above 0.364/0.398 over 701 samples. Saved untouched normalized clips upgrade, including equivalent quaternion signs and repeated-normalization noise; playback speed, real edits and deletions are protected. Checks cover 2,103 preview samples, four complete gameplay loops, crawl handoffs, existing animation/runtime/crouch-jump guards and production build. Front, oblique and side review includes lite/full rendering and confirms the saved revision loads. No full suite run. |

| 149 | Correct Unity rope arm and leg mapping | ~40 min through source inspection, retarget correction and playable motion review; release verification followed | Automatic from `main` | Repair mixed-up limbs in rope swing animations | Actual source A-pose axis calibration, Unity handedness conversion, source arm tracks, scale-aware contact IK and catalog v28 saved-clip repair | All eight arm/leg segments match independent source-direction fixtures within 0.8 degrees across hang, climb and both release clips. Removed the placeholder arm overrides. Rope contact opts into affine parent-space aiming for Character Lab scaling; unrelated IK paths remain unchanged. Saved source edits have recoverable backups and playback speeds survive migration. Focused rope/animation/IK checks, build and full-render front/oblique/side review with CRT off; no full QA suite. |

| 150 | Make the full skate trick catalogue mechanically accurate with cartoon bounce | ~65 min prompt-to-playable through contact correction and catalogue/full-render review; isolated checks and publication followed | Automatic from main | Every trick should visibly match its definition, especially nosegrinds on the front truck | Shared contact definitions, board scale compensation, measured foot/palm constraints, stance mirroring, distinct stalls, rear-foot Impossible wrap, inverted 540/900 specials, Darkslide half-flip and animated revert | Grinds load the named truck/deck surface and grabs use the correct leading/trailing hand and edge. Knees compress and rebound without moving the supporting board contact. A 37-entry local viewer exposes phases and multiple angles. Focused checks cover 9,414 pose frames, 64 mirrored/sloped grinds, 32 grabs/releases, manuals, stalls, wallrides, full flips and real gameplay catches/landings. Settled socket errors stay within 1 mm in the fixture; a before/after 3,603-frame replay has identical positions/states. Build and focused regression checks pass; no full suite run. See docs/SKATE_POSE_CONTACTS.md. |

| 151 | Relax the skating idle knees | ~8 min through implementation, focused checks and visual comparison; publication followed | Automatic from `main` | Less knee bend at skate idle to emphasize the charge crouch | Ordinary grounded skating receives a 7 cm pelvis lift, eased out during charge and trick transitions | The full-charge pelvis height remains 0.395 m above the sole midpoint; casual idle is 0.53 m. Both stances retain planted feet and a stationary board through charge/release. Existing grind, grab, manual, stall and flip contact checks remain green. Production build and CRT-free idle/charge browser comparison pass; no full QA suite. |

| 152 | Lead run inertia with fast facing and add a cartoony skid stop | ~20 min through implementation and real-controller motion review; publication followed | Automatic from `main` | Remove the slow harsh-direction body pivot and make run stops feel slidey | Four-frame reversal facing, intent-held gait, editable momentum-timed Run Stop clip and short squash/rebound settle | Physical movement is unchanged: a full release still coasts for 27 fixed frames, and facing completes while velocity is still travelling in the old direction. The skid ends with a 15-frame settle; fresh movement/jump cancels immediately and gentle analogue walking keeps its normal idle handoff. Saved suites gain only the new clip. Focused source/runtime/locomotion tests, original replay trajectory guards, build and CRT-free browser review. No full QA suite. |


| 153 | Enlarge every game menu and unify Jungle Cup | ~4 h 45 min prompt-to-playable, including reference review, responsive iteration and full-render QA; publication verification followed | Automatic from `main` | Use Crash N. Sane Trilogy and Crash 4 proportions, darker backgrounds and one consistent menu style | Shared 56 px titles / 42 px actions at 720p, deep teal panels and backdrops, larger captions/counts, and matching Jungle Cup type, focus, guides and standings | Reviewed 25 screen states at 1280×720, 1920×1080, 1024×768, 390×844 and 844×390 in Chrome. Full-render bounds/clipping audit passes all 125 combinations. All six final standings rows fit; all four guide pages and footer Back work, including touch close. Fixed inherited count sizing and the competition prompt-heading painter. Build, competition input/focus, GameFlow surface and save-preview checks pass. No full QA suite. Reference links and the local catalogue are documented in MENU_DESIGN.md. |

| 154 | Integrate idle recovery, rebuild jump loading and relax skating | ~45 min through history checks, control/pose implementation and motion review; publication verification followed | Automatic from `main` | Idle at 2×, recover limbs inside the landing/skid, restore charge concepts with better poses, reverse default mount and match standing-idle knee bend on the board | Catalog v30 tempo migration and editable Jump Charge; phase-continuous recovery into Idle during the existing bounce; dimension-based relaxed skate height and scale-aware contacts | Landing limbs reach live Idle by 0.30/0.45 s and skid limbs by 0.60/0.65 s, without a second after-recovery fade. New load keys preserve eased 35% preload, progressive hold and release-to-jump, with rearward arms, lowered gaze and planted soles; tap/full launch power remains controller-owned. Relaxed knees are lightly bent instead of the previous roughly 96° squat, with standing idle as the height reference. New mounts reverse the old stance. Saved idle keyframes remain intact. Focused controller, animation, recovery, pose-contact and replay trajectory checks plus production build and CRT-free browser review; no full QA suite. |
