import fs from 'node:fs/promises';
const art=new URL('../../art/roo-reference-match/',import.meta.url),folder=new URL('raster-v6/lights/prompts/',art);
await fs.mkdir(folder,{recursive:true});const manifest=JSON.parse(await fs.readFile(new URL('raster-v6/manifest.json',art),'utf8'));
for(const [char,master]of Object.entries(manifest.glyphs))for(const side of ['left','right']){
 const id='u'+char.codePointAt(0).toString(16).padStart(4,'0'),path=new URL(`${id}-${side}.txt`,folder);
 try{await fs.access(path);continue;}catch{}
 const emphasis=side==='left'?'upper-left edges, with a smaller reflection on existing lower-right-facing facets':'upper-right edges, with a smaller reflection on existing lower-left-facing facets';
 const prompt=`Use case: precise-object-edit, lighting variation.\nMake this exact glyph ${JSON.stringify(char)} with highlights and shadows in gently different areas. The attached already-generated beautiful neutral is the ONLY source. Its form, silhouette, openings, detached parts, bevel geometry, crisp ridges, smooth face, gold/orange/red gradient and fine detail are already approved. Keep them. Do not use a flat stencil or redesign the glyph.\nWe want a glisten that will fade with the neutral to create a convincing illusion of 3D. Shift the brightest shine toward the ${emphasis}. Only use facets that already exist; never invent openings or cuts. Let a few other current highlights become quieter and subtly rebalance shadows on existing bevel faces. Keep the broad front-face color and brightness close to the neutral. The lighting difference should be easy to see, yet gentle enough for a slow fade. It must be the same finished object in the same camera position.\nPreserve a few clean near-white specular peaks for real-time bloom later. Do NOT bake glow, halo, bloom, sparkle, outer shadow, black outline, new ridges, extra cuts, grain, stripes or blur. No warping, rotation, changed bevel widths, moved parts or lost detail. Preserve the whole original artwork and framing.\nReturn exactly the same single glyph ${JSON.stringify(char)} with only this glistening lighting variation. Keep a perfectly flat #ff00ff magenta matte outside it and inside its existing openings. No checkerboard, scene, extra text or objects.\n`;
 await fs.writeFile(path,prompt);
}
console.log(JSON.stringify({glyphs:Object.keys(manifest.glyphs).length,method:'lighting-only edits of the approved neutral'}));
