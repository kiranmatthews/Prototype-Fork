# Quaternius Walk_Loop source record

The procedural player’s editable Walk clip retains the CC0 `Walk_Loop` motion
from Quaternius’ Universal Animation Library Standard edition. It comes from
the exact same source GLB, skeleton, and conversion path as the approved
`Jog_Fwd_Loop` Run clip. Only sampled semantic keyframes ship; no Quaternius
mannequin or runtime character asset is included.

See `provenance.json` for the immutable source hash and conversion policy. The
included CC0 notice applies to the retained source animation.
